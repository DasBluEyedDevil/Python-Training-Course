{
  "lesson_id": 4,
  "module_id": 6,
  "title": "Default and Keyword Arguments",
  "duration": "30 minutes",
  "objectives": [
    "Understand how to create optional parameters with default values",
    "Learn to call functions using keyword (named) arguments",
    "Master the difference between positional and keyword arguments",
    "Write more flexible and user-friendly functions"
  ],
  "sections": [
    {
      "role": "The Simplifier",
      "title": "The Concept",
      "content": "<h3>🎨 Making Functions More Flexible</h3><p><strong>Real-World Analogy First:</strong></p><p>Imagine ordering a coffee at your local café:</p><ul><li>☕ <strong>Required:</strong> \"I'd like a coffee\" (you must specify you want coffee)</li><li>☕ <strong>Optional with defaults:</strong> Size defaults to medium, milk defaults to regular, sugar defaults to none</li><li>☕ <strong>Customize as needed:</strong> \"Make it large with oat milk and 2 sugars\"</li><li>☕ <strong>Or just accept defaults:</strong> \"Just a regular coffee\" (gets medium, regular milk, no sugar)</li></ul><p>You can also specify in any order: \"I want oat milk, large size\" or \"Large with oat milk\" - both work!</p><p>This is exactly how <strong>default arguments</strong> and <strong>keyword arguments</strong> work:</p><ul><li>🔵 <strong>Default arguments:</strong> Parameters with preset values (optional)</li><li>🔵 <strong>Keyword arguments:</strong> Calling functions using parameter names</li><li>🔵 <strong>Flexibility:</strong> Specify only what you need to change</li></ul><hr><p><strong>Now The Technical Terms:</strong></p><p><strong>Default Argument:</strong> A parameter that has a default value. If the caller doesn't provide a value, the default is used.</p><p><strong>Keyword Argument:</strong> When calling a function, you can specify arguments by parameter name (<code>size=\"large\"</code>) instead of just position.</p><p><strong>Positional Argument:</strong> Arguments matched to parameters by their position (first argument goes to first parameter, etc.).</p><p><strong>Why This Matters:</strong></p><ul><li>✅ <strong>Flexibility:</strong> Functions can have optional settings</li><li>✅ <strong>Readability:</strong> Keyword arguments make code self-documenting</li><li>✅ <strong>Convenience:</strong> Users don't need to specify every parameter</li><li>✅ <strong>Backwards compatibility:</strong> Add new optional parameters without breaking existing code</li></ul>"
    },
    {
      "role": "The Coder",
      "title": "Code Example",
      "content": "<h3>💻 Default and Keyword Arguments in Action</h3><p>Let's create functions with default parameters and call them different ways:</p>",
      "code": "# ═══════════════════════════════════════\n# EXAMPLE 1: Function with Default Arguments\n# ═══════════════════════════════════════\n\ndef greet(name, greeting=\"Hello\", punctuation=\"!\"):\n    \"\"\"Greet someone with customizable greeting and punctuation.\n    \n    Args:\n        name: Person's name (required)\n        greeting: Greeting word (default: \"Hello\")\n        punctuation: Ending punctuation (default: \"!\")\n    \"\"\"\n    message = f\"{greeting}, {name}{punctuation}\"\n    print(message)\n\nprint(\"Example 1: Default arguments allow optional parameters\")\nprint()\n\n# Using all defaults (only required argument)\ngreet(\"Alice\")  # Uses \"Hello\" and \"!\"\n\n# Override one default\ngreet(\"Bob\", \"Hi\")  # Uses \"Hi\" and \"!\"\n\n# Override both defaults\ngreet(\"Carol\", \"Hey\", \".\")\n\nprint()\n\n# ═══════════════════════════════════════\n# EXAMPLE 2: Keyword Arguments (Named Arguments)\n# ═══════════════════════════════════════\n\ndef create_user(username, email, age=18, active=True, admin=False):\n    \"\"\"Create a user account with optional settings.\n    \n    Args:\n        username: User's username (required)\n        email: User's email (required)\n        age: User's age (default: 18)\n        active: Account active status (default: True)\n        admin: Admin privileges (default: False)\n    \"\"\"\n    print(f\"Creating user: {username}\")\n    print(f\"  Email: {email}\")\n    print(f\"  Age: {age}\")\n    print(f\"  Active: {active}\")\n    print(f\"  Admin: {admin}\")\n    print()\n\nprint(\"Example 2: Keyword arguments for clarity and flexibility\")\nprint()\n\n# Positional arguments (order matters)\ncreate_user(\"alice123\", \"alice@email.com\")\n\n# Mix positional and keyword arguments\ncreate_user(\"bob456\", \"bob@email.com\", age=25)\n\n# All keyword arguments (order doesn't matter!)\ncreate_user(\n    email=\"carol@email.com\",\n    username=\"carol789\",\n    admin=True,\n    age=30\n)\n\n# Keyword arguments let you skip middle parameters\ncreate_user(\"dave\", \"dave@email.com\", admin=True)\n\nprint()\n\n# ═══════════════════════════════════════\n# EXAMPLE 3: Practical Use Case - Drawing Function\n# ═══════════════════════════════════════\n\ndef draw_box(width, height=None, border=\"*\", fill=\" \"):\n    \"\"\"Draw a box with customizable appearance.\n    \n    Args:\n        width: Width of box (required)\n        height: Height of box (defaults to same as width for square)\n        border: Character for border (default: \"*\")\n        fill: Character for interior (default: \" \")\n    \"\"\"\n    # If height not specified, make it a square\n    if height is None:\n        height = width\n    \n    # Top border\n    print(border * width)\n    \n    # Middle rows\n    for _ in range(height - 2):\n        print(border + fill * (width - 2) + border)\n    \n    # Bottom border\n    if height > 1:\n        print(border * width)\n\nprint(\"Example 3: Practical application\")\nprint()\n\nprint(\"Simple square (5x5) with defaults:\")\ndraw_box(5)\nprint()\n\nprint(\"Rectangle (8x4):\")\ndraw_box(8, 4)\nprint()\n\nprint(\"Custom border and fill:\")\ndraw_box(width=10, height=5, border=\"#\", fill=\".\")\nprint()\n\nprint(\"Different border character:\")\ndraw_box(7, border=\"+\")",
      "output": "Example 1: Default arguments allow optional parameters\n\nHello, Alice!\nHi, Bob!\nHey, Carol.\n\nExample 2: Keyword arguments for clarity and flexibility\n\nCreating user: alice123\n  Email: alice@email.com\n  Age: 18\n  Active: True\n  Admin: False\n\nCreating user: bob456\n  Email: bob@email.com\n  Age: 25\n  Active: True\n  Admin: False\n\nCreating user: carol789\n  Email: carol@email.com\n  Age: 30\n  Active: True\n  Admin: True\n\nCreating user: dave\n  Email: dave@email.com\n  Age: 18\n  Active: True\n  Admin: True\n\n\nExample 3: Practical application\n\nSimple square (5x5) with defaults:\n*****\n*   *\n*   *\n*   *\n*****\n\nRectangle (8x4):\n********\n*      *\n*      *\n********\n\nCustom border and fill:\n##########\n#........#\n#........#\n#........#\n##########\n\nDifferent border character:\n+++++++\n+     +\n+     +\n+     +\n+     +\n+++++++"
    },
    {
      "role": "The Simplifier",
      "title": "Syntax Breakdown",
      "content": "<h3>🔍 Understanding Default and Keyword Arguments</h3><p><strong>Part 1: Defining Default Arguments</strong></p><pre><code>def greet(name, greeting=\"Hello\"):\n    #          ^^^^^^^^^^^^^^^^^^^^^^^  \n    #          required  optional with default</code></pre><ul><li>📌 Syntax: <code>parameter=default_value</code></li><li>📌 Parameters WITH defaults can be omitted when calling</li><li>📌 If omitted, the default value is used</li><li>📌 If provided, your value overrides the default</li></ul><p><strong>⚠️ Important Rule: Required Before Optional</strong></p><pre><code># ✅ CORRECT: Required parameters first\ndef create_account(username, password, age=18):\n    pass\n\n# ❌ WRONG: Can't have required after optional\ndef create_account(username, age=18, password):\n    pass  # SyntaxError!</code></pre><p>Required (no default) parameters must come BEFORE optional (with default) parameters.</p><hr><p><strong>Part 2: Positional Arguments</strong></p><pre><code>def greet(name, greeting=\"Hello\", punctuation=\"!\"):\n    pass\n\n# Arguments matched by POSITION\ngreet(\"Alice\", \"Hi\", \".\")  \n#      ^^^^^   ^^^^  ^^^  \n#      name  greet punct</code></pre><ul><li>📌 Order matters!</li><li>📌 First argument goes to first parameter</li><li>📌 Second to second, etc.</li></ul><hr><p><strong>Part 3: Keyword Arguments</strong></p><pre><code># Arguments matched by NAME\ngreet(name=\"Alice\", greeting=\"Hi\", punctuation=\".\")\n#     ^^^^^^^^^^^  ^^^^^^^^^^^^^^  ^^^^^^^^^^^^^^^^  \n#     Explicit parameter names\n\n# Order doesn't matter with keyword arguments!\ngreet(punctuation=\".\", name=\"Alice\", greeting=\"Hi\")</code></pre><ul><li>📌 Use <code>parameter_name=value</code></li><li>📌 Order doesn't matter</li><li>📌 Very clear what each argument means</li><li>📌 Can skip optional parameters in the middle</li></ul><hr><p><strong>Part 4: Mixing Positional and Keyword</strong></p><pre><code># ✅ CORRECT: Positional first, then keyword\ngreet(\"Alice\", punctuation=\".\")\n#     ^^^^^^   ^^^^^^^^^^^^^^^^  \n#     positional  keyword\n\n# ❌ WRONG: Can't use positional after keyword\ngreet(name=\"Alice\", \"Hi\")  # SyntaxError!</code></pre><p><strong>Rule:</strong> Once you use a keyword argument, all following arguments must also be keyword arguments.</p><hr><p><strong>Part 5: Benefits of Keyword Arguments</strong></p><pre><code># Without keyword arguments - unclear what values mean\ncreate_user(\"alice\", \"alice@email.com\", 25, True, False)\n#           What do True and False mean? Have to check definition!\n\n# With keyword arguments - self-documenting\ncreate_user(\"alice\", \"alice@email.com\", age=25, active=True, admin=False)\n#           Now it's clear what each value represents!</code></pre><hr><p><strong>Part 6: Special Case - None as Default</strong></p><pre><code>def draw_box(width, height=None):\n    # If not provided, calculate from width\n    if height is None:\n        height = width  # Make it a square\n    # ... rest of function</code></pre><ul><li>📌 <code>None</code> is commonly used as default when you want to detect \"not provided\"</li><li>📌 Allows you to calculate defaults based on other parameters</li><li>📌 Different from <code>0</code> or <code>\"\"</code> which are actual values</li></ul><hr><p><strong>Visual Model:</strong></p><pre><code>FUNCTION DEFINITION:\ndef greet(name, greeting=\"Hello\", punctuation=\"!\"):\n          ^^^^  ^^^^^^^^^^^^^^^^  ^^^^^^^^^^^^^^^^^  \n          REQ   OPTIONAL          OPTIONAL\n\nCALLING OPTIONS:\n\n1. greet(\"Alice\")\n   → name=\"Alice\", greeting=\"Hello\", punctuation=\"!\"\n\n2. greet(\"Alice\", \"Hi\")\n   → name=\"Alice\", greeting=\"Hi\", punctuation=\"!\"\n\n3. greet(\"Alice\", punctuation=\"?\")\n   → name=\"Alice\", greeting=\"Hello\", punctuation=\"?\"\n   (skipped middle parameter using keyword!)\n\n4. greet(punctuation=\"?\", name=\"Alice\")\n   → name=\"Alice\", greeting=\"Hello\", punctuation=\"?\"\n   (order doesn't matter with all keywords!)</code></pre>"
    },
    {
      "role": "The Coder",
      "title": "Exercise",
      "content": "<h3>✏️ Your Turn: Pizza Order Function</h3><p><strong>Challenge:</strong> Create a function that takes a pizza order with customizable options.</p><p><strong>Requirements:</strong></p><ol><li>Function name: <code>order_pizza</code></li><li>Parameters:<ul><li><code>size</code> (required): \"small\", \"medium\", or \"large\"</li><li><code>crust</code> (optional, default=\"regular\"): Type of crust</li><li><code>cheese</code> (optional, default=\"mozzarella\"): Type of cheese</li><li><code>toppings</code> (optional, default=[]): List of toppings</li><li><code>delivery</code> (optional, default=False): Delivery or pickup</li></ul></li><li>The function should:<ul><li>Print a formatted order summary</li><li>Calculate and return the total price</li><li>Base prices: small=$8, medium=$12, large=$16</li><li>Add $2 per topping</li><li>Add $3 if delivery is True</li></ul></li><li>Include a docstring</li></ol><p><strong>Test Cases:</strong></p><pre><code># Basic order\ntotal = order_pizza(\"medium\")\n\n# Custom order with keyword arguments\ntotal = order_pizza(\n    size=\"large\",\n    crust=\"thin\",\n    toppings=[\"pepperoni\", \"mushrooms\", \"olives\"],\n    delivery=True\n)\n\n# Another custom order\ntotal = order_pizza(\"small\", cheese=\"vegan\", toppings=[\"peppers\"])</code></pre><p><strong>Hint:</strong> Remember that empty lists as default arguments need special handling! Use <code>None</code> as default and create empty list inside function if needed.</p>",
      "starter_code": "def order_pizza(size, crust=\"regular\", cheese=\"mozzarella\", toppings=None, delivery=False):\n    \"\"\"Take a pizza order and calculate the total price.\n    \n    Args:\n        size: Pizza size - \"small\", \"medium\", or \"large\" (required)\n        crust: Type of crust (default: \"regular\")\n        cheese: Type of cheese (default: \"mozzarella\")\n        toppings: List of toppings (default: [])\n        delivery: Delivery or pickup (default: False)\n    \n    Returns:\n        Total price of the order\n    \"\"\"\n    # Handle default for toppings\n    if toppings is None:\n        toppings = []\n    \n    # Your code here:\n    # 1. Print order summary\n    # 2. Calculate price\n    # 3. Return total\n    \n    pass\n\n# Test your function\nprint(\"🍕 PIZZA PALACE\")\nprint(\"═\" * 40)\n# Your test code here\n"
    },
    {
      "role": "The Coder",
      "title": "Solution",
      "content": "<h3>✅ Solution: Pizza Order Function</h3>",
      "code": "def order_pizza(size, crust=\"regular\", cheese=\"mozzarella\", toppings=None, delivery=False):\n    \"\"\"Take a pizza order and calculate the total price.\n    \n    Args:\n        size: Pizza size - \"small\", \"medium\", or \"large\" (required)\n        crust: Type of crust (default: \"regular\")\n        cheese: Type of cheese (default: \"mozzarella\")\n        toppings: List of toppings (default: [])\n        delivery: Delivery or pickup (default: False)\n    \n    Returns:\n        Total price of the order\n    \"\"\"\n    # Handle default for toppings (important!)\n    if toppings is None:\n        toppings = []\n    \n    # Base prices\n    prices = {\"small\": 8, \"medium\": 12, \"large\": 16}\n    base_price = prices.get(size, 12)  # Default to medium if invalid size\n    \n    # Calculate total\n    total = base_price\n    total += len(toppings) * 2  # $2 per topping\n    if delivery:\n        total += 3  # $3 delivery fee\n    \n    # Print order summary\n    print(\"\\n\" + \"─\" * 40)\n    print(f\"🍕 PIZZA ORDER\")\n    print(\"─\" * 40)\n    print(f\"Size:     {size.capitalize()}\")\n    print(f\"Crust:    {crust.capitalize()}\")\n    print(f\"Cheese:   {cheese.capitalize()}\")\n    \n    if toppings:\n        print(f\"Toppings: {', '.join(toppings).title()}\")\n    else:\n        print(f\"Toppings: None (cheese pizza)\")\n    \n    print(f\"Delivery: {'Yes' if delivery else 'No (Pickup)'}\")\n    print(\"─\" * 40)\n    print(f\"Base price: ${base_price:.2f}\")\n    \n    if toppings:\n        print(f\"Toppings:   ${len(toppings) * 2:.2f} ({len(toppings)} × $2)\")\n    \n    if delivery:\n        print(f\"Delivery:   $3.00\")\n    \n    print(f\"TOTAL:      ${total:.2f}\")\n    print(\"─\" * 40)\n    \n    return total\n\n\n# ═══════════════════════════════════════\n# TESTING THE PIZZA ORDERING SYSTEM\n# ═══════════════════════════════════════\n\nprint(\"🍕 PIZZA PALACE - ORDER SYSTEM\")\nprint(\"═\" * 40)\n\nprint(\"\\n1️⃣ Basic order (all defaults except size):\")\ntotal1 = order_pizza(\"medium\")\nprint(f\"\\n💰 Paid: ${total1:.2f}\")\n\nprint(\"\\n\\n2️⃣ Custom order with keyword arguments:\")\ntotal2 = order_pizza(\n    size=\"large\",\n    crust=\"thin\",\n    toppings=[\"pepperoni\", \"mushrooms\", \"olives\"],\n    delivery=True\n)\nprint(f\"\\n💰 Paid: ${total2:.2f}\")\n\nprint(\"\\n\\n3️⃣ Small pizza with special cheese:\")\ntotal3 = order_pizza(\"small\", cheese=\"vegan\", toppings=[\"peppers\"])\nprint(f\"\\n💰 Paid: ${total3:.2f}\")\n\nprint(\"\\n\\n4️⃣ Large supreme with all the works:\")\ntotal4 = order_pizza(\n    \"large\",\n    \"stuffed\",\n    toppings=[\"pepperoni\", \"sausage\", \"peppers\", \"onions\", \"mushrooms\"],\n    delivery=True\n)\nprint(f\"\\n💰 Paid: ${total4:.2f}\")\n\nprint(\"\\n\\n5️⃣ Using all keyword arguments (any order!):\")\ntotal5 = order_pizza(\n    delivery=True,\n    toppings=[\"pineapple\", \"ham\"],\n    size=\"medium\",\n    crust=\"pan\"\n)\nprint(f\"\\n💰 Paid: ${total5:.2f}\")\n\nprint(\"\\n\\n\" + \"═\" * 40)\nprint(f\"📊 TOTAL SALES: ${total1 + total2 + total3 + total4 + total5:.2f}\")\nprint(\"═\" * 40)",
      "output": "🍕 PIZZA PALACE - ORDER SYSTEM\n════════════════════════════════════════\n\n1️⃣ Basic order (all defaults except size):\n\n────────────────────────────────────────\n🍕 PIZZA ORDER\n────────────────────────────────────────\nSize:     Medium\nCrust:    Regular\nCheese:   Mozzarella\nToppings: None (cheese pizza)\nDelivery: No (Pickup)\n────────────────────────────────────────\nBase price: $12.00\nTOTAL:      $12.00\n────────────────────────────────────────\n\n💰 Paid: $12.00\n\n\n2️⃣ Custom order with keyword arguments:\n\n────────────────────────────────────────\n🍕 PIZZA ORDER\n────────────────────────────────────────\nSize:     Large\nCrust:    Thin\nCheese:   Mozzarella\nToppings: Pepperoni, Mushrooms, Olives\nDelivery: Yes\n────────────────────────────────────────\nBase price: $16.00\nToppings:   $6.00 (3 × $2)\nDelivery:   $3.00\nTOTAL:      $25.00\n────────────────────────────────────────\n\n💰 Paid: $25.00\n\n\n3️⃣ Small pizza with special cheese:\n\n────────────────────────────────────────\n🍕 PIZZA ORDER\n────────────────────────────────────────\nSize:     Small\nCrust:    Regular\nCheese:   Vegan\nToppings: Peppers\nDelivery: No (Pickup)\n────────────────────────────────────────\nBase price: $8.00\nToppings:   $2.00 (1 × $2)\nTOTAL:      $10.00\n────────────────────────────────────────\n\n💰 Paid: $10.00\n\n\n4️⃣ Large supreme with all the works:\n\n────────────────────────────────────────\n🍕 PIZZA ORDER\n────────────────────────────────────────\nSize:     Large\nCrust:    Stuffed\nCheese:   Mozzarella\nToppings: Pepperoni, Sausage, Peppers, Onions, Mushrooms\nDelivery: Yes\n────────────────────────────────────────\nBase price: $16.00\nToppings:   $10.00 (5 × $2)\nDelivery:   $3.00\nTOTAL:      $29.00\n────────────────────────────────────────\n\n💰 Paid: $29.00\n\n\n5️⃣ Using all keyword arguments (any order!):\n\n────────────────────────────────────────\n🍕 PIZZA ORDER\n────────────────────────────────────────\nSize:     Medium\nCrust:    Pan\nCheese:   Mozzarella\nToppings: Pineapple, Ham\nDelivery: Yes\n────────────────────────────────────────\nBase price: $12.00\nToppings:   $4.00 (2 × $2)\nDelivery:   $3.00\nTOTAL:      $19.00\n────────────────────────────────────────\n\n💰 Paid: $19.00\n\n\n════════════════════════════════════════\n📊 TOTAL SALES: $95.00\n════════════════════════════════════════",
      "explanation": "<h3>📚 How It Works:</h3><ol><li><strong>Function Signature (Line 1):</strong><ul><li><code>size</code> is REQUIRED (no default value)</li><li><code>crust=\"regular\"</code> is OPTIONAL (has default)</li><li><code>cheese=\"mozzarella\"</code> is OPTIONAL</li><li><code>toppings=None</code> is OPTIONAL (None, not []!)</li><li><code>delivery=False</code> is OPTIONAL</li></ul></li><li><strong>Why toppings=None Instead of toppings=[]:</strong><pre><code># ❌ DANGEROUS - mutable default argument\ndef order_pizza(..., toppings=[]):\n    # Same list object reused for all calls!\n    # Toppings from previous orders persist!\n\n# ✅ SAFE - use None and create new list each time\ndef order_pizza(..., toppings=None):\n    if toppings is None:\n        toppings = []  # Fresh list each time</code></pre><p>This is a Python gotcha! Mutable default arguments (lists, dicts) are created once and shared across all function calls.</p></li><li><strong>Flexible Calling Patterns:</strong><ul><li><code>order_pizza(\"medium\")</code> - Just required, all defaults</li><li><code>order_pizza(\"large\", \"thin\")</code> - Positional arguments</li><li><code>order_pizza(\"small\", cheese=\"vegan\")</code> - Mix positional and keyword</li><li><code>order_pizza(size=\"large\", delivery=True, toppings=[...])</code> - All keyword, any order</li></ul></li><li><strong>Price Calculation (Lines 19-24):</strong><ul><li>Base price from size</li><li>Add $2 per topping (<code>len(toppings) * 2</code>)</li><li>Add $3 if delivery is True</li></ul></li><li><strong>Formatted Output (Lines 27-50):</strong><ul><li>Shows complete order summary</li><li>Breaks down pricing</li><li>Makes it easy to verify order is correct</li></ul></li></ol><h3>⚠️ Common Mistakes:</h3><ol><li><strong>Mutable Default Arguments:</strong><pre><code># ❌ WRONG - dangerous bug!\ndef order_pizza(size, toppings=[]):\n    toppings.append(\"cheese\")  # Modifies shared list!\n    return toppings\n\norder_pizza(\"medium\")  # [\"cheese\"]\norder_pizza(\"large\")   # [\"cheese\", \"cheese\"] - WAT?!\n\n# ✅ CORRECT\ndef order_pizza(size, toppings=None):\n    if toppings is None:\n        toppings = []\n    toppings.append(\"cheese\")  # Modifies fresh list\n    return toppings</code></pre></li><li><strong>Required After Optional:</strong><pre><code># ❌ WRONG - SyntaxError\ndef order_pizza(size=\"medium\", crust):\n    pass\n\n# ✅ CORRECT - required first\ndef order_pizza(crust, size=\"medium\"):\n    pass</code></pre></li><li><strong>Positional After Keyword:</strong><pre><code># ❌ WRONG - SyntaxError\norder_pizza(size=\"large\", \"thin\")\n\n# ✅ CORRECT\norder_pizza(\"large\", \"thin\")  # All positional\norder_pizza(\"large\", crust=\"thin\")  # Mix OK if positional first</code></pre></li><li><strong>Forgetting to Return:</strong><pre><code># ❌ WRONG - function does work but doesn't return total\ndef order_pizza(size):\n    total = calculate_price(size)\n    print(total)  # Prints but doesn't return!\n\n# ✅ CORRECT\ndef order_pizza(size):\n    total = calculate_price(size)\n    print(total)\n    return total  # Now caller can use the value</code></pre></li></ol><h3>💡 Pro Tips:</h3><ul><li>✅ <strong>Use keyword arguments for clarity:</strong> <code>order_pizza(\"large\", delivery=True)</code> is clearer than <code>order_pizza(\"large\", \"regular\", \"mozzarella\", [], True)</code></li><li>✅ <strong>None for mutable defaults:</strong> Always use <code>None</code> for list/dict defaults</li><li>✅ <strong>Descriptive parameter names:</strong> <code>delivery=True</code> is clearer than <code>d=True</code></li><li>✅ <strong>Document defaults in docstring:</strong> Help users know what defaults are</li><li>✅ <strong>Optional parameters = flexibility:</strong> Start with reasonable defaults, let users customize</li></ul>"
    },
    {
      "role": "Summary",
      "title": "Key Takeaways",
      "content": "<h3>🎯 Essential Points to Remember:</h3><ol><li><strong>Default arguments make parameters optional:</strong><pre><code>def greet(name, greeting=\"Hello\"):\n    #                ^^^^^^^^^^^^^^^  \n    #                Optional with default value</code></pre><ul><li>Parameters with <code>=value</code> are optional</li><li>If not provided, the default is used</li><li>If provided, your value overrides the default</li></ul></li><li><strong>Required parameters must come first:</strong><pre><code># ✅ CORRECT\ndef func(required, optional=\"default\"):\n\n# ❌ WRONG\ndef func(optional=\"default\", required):</code></pre></li><li><strong>Three ways to pass arguments:</strong><ul><li><strong>Positional:</strong> <code>greet(\"Alice\", \"Hi\")</code> - order matters</li><li><strong>Keyword:</strong> <code>greet(name=\"Alice\", greeting=\"Hi\")</code> - order doesn't matter</li><li><strong>Mixed:</strong> <code>greet(\"Alice\", greeting=\"Hi\")</code> - positional must come first</li></ul></li><li><strong>Keyword arguments improve readability:</strong><pre><code># Hard to understand\ncreate_user(\"alice\", \"alice@email.com\", 25, True, False)\n\n# Self-documenting\ncreate_user(\"alice\", \"alice@email.com\", age=25, active=True, admin=False)</code></pre></li><li><strong>Keyword arguments allow skipping middle parameters:</strong><pre><code>def func(a, b=\"default_b\", c=\"default_c\", d=\"default_d\"):\n    pass\n\n# Can skip b and c, just change d\nfunc(\"value_a\", d=\"value_d\")</code></pre></li><li><strong>Mutable default argument pitfall:</strong><pre><code># ❌ DANGEROUS - don't use mutable defaults directly\ndef append_to(item, list=[]):\n    list.append(item)\n    return list\n\n# ✅ SAFE - use None and create inside\ndef append_to(item, list=None):\n    if list is None:\n        list = []\n    list.append(item)\n    return list</code></pre></li><li><strong>None is useful for \"not provided\" detection:</strong><ul><li>Different from <code>0</code>, <code>\"\"</code>, <code>False</code> which are actual values</li><li>Allows calculating defaults based on other parameters</li></ul></li><li><strong>Mixing rules:</strong><ul><li>Once you use a keyword argument, all following must be keyword</li><li>Can't do: <code>func(name=\"Alice\", \"Hi\")</code> - positional after keyword</li><li>Can do: <code>func(\"Alice\", greeting=\"Hi\")</code> - keyword after positional</li></ul></li><li><strong>Benefits of defaults and keywords:</strong><ul><li>More flexible functions</li><li>Better code readability</li><li>Easier to add new features without breaking existing code</li><li>Users only specify what they need to change</li></ul></li></ol><h3>🚀 Coming Up Next:</h3><p>In the next lesson, you'll learn about <strong>lambda functions</strong> (anonymous functions) and explore Python's powerful built-in functions like <code>map()</code>, <code>filter()</code>, and <code>sorted()</code> that make your code more concise and Pythonic!</p>"
    }
  ],
  "checkpoint_quiz": [
    {
      "question": "What happens if you don't provide an argument for a parameter that has a default value?",
      "options": [
        "You get a TypeError",
        "The function doesn't run",
        "The default value is used automatically",
        "The parameter becomes None"
      ],
      "correct_answer": 2,
      "explanation": "The default value is used automatically! That's the whole point of default arguments - they make parameters optional by providing a fallback value. If you don't provide a value when calling, Python uses the default from the function definition."
    },
    {
      "question": "Which function call is INVALID?\n\ndef greet(name, greeting=\"Hello\", punct=\"!\"):\n    pass",
      "options": [
        "greet(\"Alice\")",
        "greet(\"Alice\", \"Hi\")",
        "greet(name=\"Alice\", \"Hi\")",
        "greet(\"Alice\", punct=\"?\")"
      ],
      "correct_answer": 2,
      "explanation": "Option C is INVALID because you cannot use a positional argument (\"Hi\") after a keyword argument (name=\"Alice\"). Once you start using keyword arguments, all following arguments must also be keywords. Options A, B, and D are all valid."
    },
    {
      "question": "Why should you avoid using an empty list [] as a default argument?",
      "options": [
        "It causes a SyntaxError",
        "Lists can't be default arguments",
        "The same list object is shared across all function calls",
        "It makes the function run slower"
      ],
      "correct_answer": 2,
      "explanation": "The same list object is shared across ALL function calls! Mutable default arguments (lists, dicts) are created ONCE when the function is defined, not each time it's called. This can cause unexpected bugs where data from previous calls persists. Use 'None' as default and create a new list inside the function."
    }
  ]
}
