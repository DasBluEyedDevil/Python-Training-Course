{
  "lesson_id": 2,
  "module_id": 6,
  "title": "Parameters and Return Values",
  "duration": "30 minutes",
  "objectives": [
    "Understand how to send information INTO functions using parameters",
    "Learn how to get information OUT of functions using return values",
    "Master the difference between parameters and arguments",
    "Create flexible functions that work with different inputs"
  ],
  "sections": [
    {
      "role": "The Simplifier",
      "title": "The Concept",
      "content": "<h3>🎰 Functions: The Input-Process-Output Machine</h3><p><strong>Real-World Analogy First:</strong></p><p>Imagine a smoothie blender at a juice bar:</p><ul><li>🍓 <strong>INPUT:</strong> You put in strawberries, banana, and yogurt</li><li>⚙️ <strong>PROCESS:</strong> The blender mixes them together</li><li>🥤 <strong>OUTPUT:</strong> You get a delicious smoothie</li></ul><p>The ingredients you put in are different each time (maybe today you want mango instead of strawberries), but the blender always does the same process. The result you get depends on what you put in!</p><p>This is exactly how function <strong>parameters</strong> and <strong>return values</strong> work:</p><ul><li>🔵 <strong>Parameters</strong> are the INPUTS (ingredients)</li><li>🔵 <strong>Return values</strong> are the OUTPUTS (smoothie)</li><li>🔵 The function is the PROCESS (blending)</li></ul><hr><p><strong>Now The Technical Terms:</strong></p><p><strong>Parameters:</strong> Variables in the function definition that receive input values. They're like empty containers waiting to be filled.</p><p><strong>Arguments:</strong> The actual values you pass when calling the function. They're what fills those containers.</p><p><strong>Return Value:</strong> The result that comes back from the function. Functions can send information back to the code that called them.</p><p><strong>Why This Matters:</strong></p><ul><li>✅ <strong>Flexibility:</strong> One function works with many different inputs</li><li>✅ <strong>Reusability:</strong> Same function for different data</li><li>✅ <strong>Communication:</strong> Functions can share results with other parts of your program</li><li>✅ <strong>Testability:</strong> Give input, check output - easy to test!</li></ul>"
    },
    {
      "role": "The Coder",
      "title": "Code Example",
      "content": "<h3>💻 Functions With Parameters and Return Values</h3><p>Let's create functions that accept inputs and produce outputs:</p>",
      "code": "# Function WITH parameters (accepts inputs)\ndef greet_person(name):\n    \"\"\"Greet a specific person by name.\"\"\"\n    print(f\"Hello, {name}! Welcome to Python functions!\")\n\n# Function WITH parameters AND return value\ndef calculate_area(length, width):\n    \"\"\"Calculate the area of a rectangle and return the result.\"\"\"\n    area = length * width\n    return area  # Send the result back\n\n# Function that returns a value directly\ndef double_number(num):\n    \"\"\"Return double the given number.\"\"\"\n    return num * 2\n\n# Function with multiple return values\ndef get_name_parts(full_name):\n    \"\"\"Split a full name into first and last name.\"\"\"\n    parts = full_name.split()\n    first = parts[0]\n    last = parts[-1]\n    return first, last  # Returns a tuple with two values\n\n\n# ═══════════════════════════════════════\n# USING THE FUNCTIONS\n# ═══════════════════════════════════════\n\nprint(\"1️⃣ Function with parameter (input):\")\ngreet_person(\"Alice\")  # \"Alice\" is the argument\ngreet_person(\"Bob\")    # \"Bob\" is the argument\nprint()\n\nprint(\"2️⃣ Function with parameters and return value:\")\nresult = calculate_area(5, 3)  # Arguments: 5 and 3\nprint(f\"Area of 5 x 3 rectangle: {result}\")\n\nresult2 = calculate_area(10, 7)\nprint(f\"Area of 10 x 7 rectangle: {result2}\")\nprint()\n\nprint(\"3️⃣ Using return value in expressions:\")\ndoubled = double_number(7)\nprint(f\"7 doubled is {doubled}\")\nprint(f\"20 doubled is {double_number(20)}\")  # Use directly!\nprint()\n\nprint(\"4️⃣ Multiple return values:\")\nfirst_name, last_name = get_name_parts(\"Alice Johnson\")\nprint(f\"First: {first_name}\")\nprint(f\"Last: {last_name}\")",
      "output": "1️⃣ Function with parameter (input):\nHello, Alice! Welcome to Python functions!\nHello, Bob! Welcome to Python functions!\n\n2️⃣ Function with parameters and return value:\nArea of 5 x 3 rectangle: 15\nArea of 10 x 7 rectangle: 70\n\n3️⃣ Using return value in expressions:\n7 doubled is 14\n20 doubled is 40\n\n4️⃣ Multiple return values:\nFirst: Alice\nLast: Johnson"
    },
    {
      "role": "The Simplifier",
      "title": "Syntax Breakdown",
      "content": "<h3>🔍 Understanding Parameters and Return</h3><p><strong>Part 1: Function Parameters (Inputs)</strong></p><pre><code>def greet_person(name):\n    print(f\"Hello, {name}!\")</code></pre><ul><li>📌 <code>name</code> is a <strong>parameter</strong> - a variable that receives a value</li><li>📌 Parameters go inside the parentheses in the function definition</li><li>📌 You can have multiple parameters: <code>def calculate_area(length, width):</code></li><li>📌 Parameters are separated by commas</li><li>📌 These are like empty boxes waiting to receive values</li></ul><p><strong>Part 2: Calling With Arguments</strong></p><pre><code>greet_person(\"Alice\")  # \"Alice\" is an argument</code></pre><ul><li>📌 <code>\"Alice\"</code> is an <strong>argument</strong> - the actual value you pass</li><li>📌 When you call the function, the argument fills the parameter</li><li>📌 Inside the function, <code>name</code> becomes <code>\"Alice\"</code></li><li>📌 Different call, different argument: <code>greet_person(\"Bob\")</code> makes <code>name</code> become <code>\"Bob\"</code></li></ul><p><strong>Memory Trick:</strong> <strong>P</strong>arameters are in the definition (both start with P-words: \"Put\" in definition). <strong>A</strong>rguments are when you call (both start with A-words: \"Actual\" values).</p><hr><p><strong>Part 3: The Return Statement</strong></p><pre><code>def calculate_area(length, width):\n    area = length * width\n    return area  # Send result back</code></pre><ul><li>📌 <code>return</code> keyword sends a value back to the caller</li><li>📌 The function immediately exits when it hits <code>return</code></li><li>📌 The return value can be stored: <code>result = calculate_area(5, 3)</code></li><li>📌 Or used directly: <code>print(calculate_area(5, 3))</code></li></ul><p><strong>Part 4: Functions Without Return</strong></p><pre><code>def greet_person(name):\n    print(f\"Hello, {name}!\")\n    # No return statement</code></pre><ul><li>📌 If no <code>return</code> statement, the function returns <code>None</code></li><li>📌 <code>None</code> is Python's way of saying \"no value\"</li><li>📌 These functions DO something (side effect) but don't give back a result</li></ul><p><strong>Part 5: Multiple Return Values</strong></p><pre><code>def get_name_parts(full_name):\n    # ... processing ...\n    return first, last  # Returns a tuple</code></pre><ul><li>📌 Separate values with commas: <code>return first, last</code></li><li>📌 Python packs them into a tuple automatically</li><li>📌 Unpack when calling: <code>first_name, last_name = get_name_parts(\"Alice Johnson\")</code></li></ul><hr><p><strong>Visual Model:</strong></p><pre><code>         CALLING CODE              FUNCTION\n         ═══════════════════════════════════\n\n    result = calculate_area(5, 3)\n                   ↓        ↓  ↓\n                   ↓    length width\n                   ↓        ↓  ↓\n                   ↓    def calculate_area(length, width):\n                   ↓        area = length * width\n                   ↓        return area\n                   ↓           ↓\n                   ↓═══════════↓ (returns 15)\n                   ↓\n               result = 15</code></pre><p><strong>⚠️ Common Confusion:</strong></p><pre><code># ❌ WRONG - using print instead of return\ndef add(a, b):\n    print(a + b)  # This displays but doesn't RETURN\n\nresult = add(5, 3)  # result is None, not 8!\n\n# ✅ CORRECT - using return\ndef add(a, b):\n    return a + b  # This returns the value\n\nresult = add(5, 3)  # result is 8</code></pre>"
    },
    {
      "role": "The Coder",
      "title": "Exercise",
      "content": "<h3>✏️ Your Turn: Temperature Converter</h3><p><strong>Challenge:</strong> Create a function that converts Celsius to Fahrenheit and returns the result.</p><p><strong>Formula:</strong> <code>fahrenheit = (celsius * 9/5) + 32</code></p><p><strong>Requirements:</strong></p><ol><li>Function name: <code>celsius_to_fahrenheit</code></li><li>One parameter: <code>celsius</code> (the temperature in Celsius)</li><li>Calculate the Fahrenheit equivalent using the formula</li><li>Return the result (don't just print it!)</li><li>Include a docstring</li><li>Test with at least 3 different temperatures</li></ol><p><strong>Example Usage:</strong></p><pre><code>temp_f = celsius_to_fahrenheit(0)\nprint(f\"0°C = {temp_f}°F\")  # Should print: 0°C = 32.0°F\n\ntemp_f = celsius_to_fahrenheit(100)\nprint(f\"100°C = {temp_f}°F\")  # Should print: 100°C = 212.0°F</code></pre><p><strong>Bonus Challenge:</strong> Create a second function <code>fahrenheit_to_celsius</code> that does the reverse conversion!</p><p>Formula: <code>celsius = (fahrenheit - 32) * 5/9</code></p>",
      "starter_code": "# Define your celsius_to_fahrenheit function\ndef celsius_to_fahrenheit(celsius):\n    # Your docstring here\n    \n    # Calculate fahrenheit using the formula\n    \n    # Return the result\n    pass  # Remove this when you add your code\n\n# BONUS: Define fahrenheit_to_celsius function\ndef fahrenheit_to_celsius(fahrenheit):\n    # Your code here\n    pass\n\n# Test your function with different values\n# Your test code here\n"
    },
    {
      "role": "The Coder",
      "title": "Solution",
      "content": "<h3>✅ Solution: Temperature Converter</h3>",
      "code": "# Main challenge: Celsius to Fahrenheit\ndef celsius_to_fahrenheit(celsius):\n    \"\"\"Convert temperature from Celsius to Fahrenheit.\n    \n    Args:\n        celsius: Temperature in degrees Celsius\n    \n    Returns:\n        Temperature in degrees Fahrenheit\n    \"\"\"\n    fahrenheit = (celsius * 9/5) + 32\n    return fahrenheit\n\n# Bonus: Fahrenheit to Celsius\ndef fahrenheit_to_celsius(fahrenheit):\n    \"\"\"Convert temperature from Fahrenheit to Celsius.\n    \n    Args:\n        fahrenheit: Temperature in degrees Fahrenheit\n    \n    Returns:\n        Temperature in degrees Celsius\n    \"\"\"\n    celsius = (fahrenheit - 32) * 5/9\n    return celsius\n\n# ═══════════════════════════════════════\n# TESTING THE FUNCTIONS\n# ═══════════════════════════════════════\n\nprint(\"🌡️  CELSIUS TO FAHRENHEIT\")\nprint(\"═\" * 40)\n\n# Test 1: Freezing point of water\ntemp_f = celsius_to_fahrenheit(0)\nprint(f\"0°C = {temp_f}°F\")\n\n# Test 2: Boiling point of water\ntemp_f = celsius_to_fahrenheit(100)\nprint(f\"100°C = {temp_f}°F\")\n\n# Test 3: Normal body temperature\ntemp_f = celsius_to_fahrenheit(37)\nprint(f\"37°C = {temp_f}°F\")\n\n# Test 4: Nice spring day\ntemp_f = celsius_to_fahrenheit(20)\nprint(f\"20°C = {temp_f}°F\")\n\nprint(\"\\n🌡️  FAHRENHEIT TO CELSIUS (BONUS)\")\nprint(\"═\" * 40)\n\n# Reverse conversions\ntemp_c = fahrenheit_to_celsius(32)\nprint(f\"32°F = {temp_c}°C\")\n\ntemp_c = fahrenheit_to_celsius(212)\nprint(f\"212°F = {temp_c}°C\")\n\ntemp_c = fahrenheit_to_celsius(98.6)\nprint(f\"98.6°F = {temp_c}°C\")\n\nprint(\"\\n🔄 ROUND TRIP TEST (Should get back original):\")\nprint(\"═\" * 40)\noriginal = 25\nto_f = celsius_to_fahrenheit(original)\nback_to_c = fahrenheit_to_celsius(to_f)\nprint(f\"{original}°C → {to_f}°F → {back_to_c}°C\")\nprint(f\"Match: {abs(original - back_to_c) < 0.0001}\")  # Check if equal",
      "output": "🌡️  CELSIUS TO FAHRENHEIT\n════════════════════════════════════════\n0°C = 32.0°F\n100°C = 212.0°F\n37°C = 98.6°F\n20°C = 68.0°F\n\n🌡️  FAHRENHEIT TO CELSIUS (BONUS)\n════════════════════════════════════════\n32°F = 0.0°C\n212°F = 100.0°C\n98.6°F = 37.0°C\n\n🔄 ROUND TRIP TEST (Should get back original):\n════════════════════════════════════════\n25°C → 77.0°F → 25.0°C\nMatch: True",
      "explanation": "<h3>📚 How It Works:</h3><ol><li><strong>Function Structure (Lines 1-11):</strong><ul><li><code>def celsius_to_fahrenheit(celsius):</code> defines function with ONE parameter</li><li>The parameter <code>celsius</code> receives whatever value we pass</li><li>Line 10: We calculate using the formula</li><li>Line 11: We <code>return</code> the result (not print!)</li><li>Extended docstring explains parameters and return value</li></ul></li><li><strong>The Return Statement:</strong><ul><li>When we call <code>celsius_to_fahrenheit(0)</code>, the function calculates <code>(0 * 9/5) + 32 = 32.0</code></li><li>The <code>return</code> statement sends <code>32.0</code> back</li><li>We can store it: <code>temp_f = celsius_to_fahrenheit(0)</code></li><li>Or use directly: <code>print(celsius_to_fahrenheit(0))</code></li></ul></li><li><strong>Bonus Function (Lines 13-23):</strong><ul><li>Same structure, different formula</li><li>Shows how functions can be inverses of each other</li></ul></li><li><strong>Testing (Lines 25-57):</strong><ul><li>Test with known values (0°C = 32°F, 100°C = 212°F)</li><li>Use the return values in f-strings</li><li>Round-trip test: convert C→F→C to verify accuracy</li></ul></li></ol><h3>⚠️ Common Mistakes:</h3><ol><li><strong>Using print() Instead of return:</strong><pre><code># ❌ WRONG - prints but doesn't return\ndef celsius_to_fahrenheit(celsius):\n    fahrenheit = (celsius * 9/5) + 32\n    print(fahrenheit)  # Can't store or use this!\n\nresult = celsius_to_fahrenheit(0)  # result is None!\n\n# ✅ CORRECT - returns the value\ndef celsius_to_fahrenheit(celsius):\n    fahrenheit = (celsius * 9/5) + 32\n    return fahrenheit  # Can store and use this</code></pre></li><li><strong>Forgetting to Return:</strong><pre><code># ❌ WRONG - calculates but doesn't return\ndef celsius_to_fahrenheit(celsius):\n    fahrenheit = (celsius * 9/5) + 32\n    # Forgot return statement!\n\nresult = celsius_to_fahrenheit(0)  # result is None!</code></pre></li><li><strong>Wrong Number of Arguments:</strong><pre><code># ❌ WRONG - function expects 1 argument\ncelsius_to_fahrenheit()  # TypeError: missing required argument\ncelsius_to_fahrenheit(0, 32)  # TypeError: too many arguments\n\n# ✅ CORRECT - exactly 1 argument\ncelsius_to_fahrenheit(0)</code></pre></li><li><strong>Not Using the Return Value:</strong><pre><code># ❌ WASTEFUL - function returns value but we ignore it\ncelsius_to_fahrenheit(100)  # Calculates but result is lost\n\n# ✅ CORRECT - store or use the return value\ntemp = celsius_to_fahrenheit(100)\nprint(f\"Temperature: {celsius_to_fahrenheit(100)}°F\")</code></pre></li><li><strong>Variable Name Confusion:</strong><pre><code># ❌ CONFUSING - reusing parameter name\ncelsius = 100\ncelsius = celsius_to_fahrenheit(celsius)  # Works but confusing\n\n# ✅ CLEAR - different names for input and output\ncelsius = 100\nfahrenheit = celsius_to_fahrenheit(celsius)</code></pre></li></ol><h3>💡 Pro Tips:</h3><ul><li>✅ <strong>Return, don't print:</strong> Functions should return values so other code can use them</li><li>✅ <strong>Test with known values:</strong> 0°C = 32°F, 100°C = 212°F are easy to verify</li><li>✅ <strong>Descriptive names:</strong> <code>celsius_to_fahrenheit</code> clearly shows what it does</li><li>✅ <strong>Document parameters:</strong> Use docstrings to explain what inputs are expected</li><li>✅ <strong>One calculation per function:</strong> Each function does one clear task</li></ul>"
    },
    {
      "role": "Summary",
      "title": "Key Takeaways",
      "content": "<h3>🎯 Essential Points to Remember:</h3><ol><li><strong>Parameters vs Arguments:</strong><ul><li><strong>Parameters:</strong> Variables in the function definition (<code>def greet(name):</code>)</li><li><strong>Arguments:</strong> Actual values when calling (<code>greet(\"Alice\")</code>)</li><li>Think: Parameters are empty boxes, arguments fill them</li></ul></li><li><strong>Functions can accept inputs:</strong><pre><code>def function_name(parameter1, parameter2):\n    # Use the parameters inside</code></pre><ul><li>Parameters make functions flexible and reusable</li><li>Same function works with different data</li></ul></li><li><strong>The return statement sends values back:</strong><pre><code>def calculate(x):\n    result = x * 2\n    return result  # Send back to caller</code></pre><ul><li>Use <code>return</code> to give results back to the calling code</li><li>The function exits immediately when hitting <code>return</code></li></ul></li><li><strong>Return vs Print:</strong><ul><li><code>print()</code> displays to screen (side effect, no value back)</li><li><code>return</code> sends value back to the code (can be stored/used)</li><li>Functions should usually <code>return</code> values, not <code>print</code> them</li></ul></li><li><strong>No return statement = returns None:</strong><ul><li>Functions without <code>return</code> automatically return <code>None</code></li><li>These functions DO something (side effect) but don't provide a result</li></ul></li><li><strong>Multiple return values:</strong><pre><code>return value1, value2, value3  # Returns tuple</code></pre><ul><li>Separate with commas to return multiple values</li><li>Unpack when receiving: <code>a, b, c = function()</code></li></ul></li><li><strong>Using return values:</strong><pre><code>result = calculate_area(5, 3)  # Store it\nprint(calculate_area(5, 3))     # Use directly\nif calculate_area(5, 3) > 10:   # In conditions</code></pre></li><li><strong>Function signature:</strong><ul><li>The first line of a function (<code>def name(params):</code>) is called the signature</li><li>It tells you what inputs the function needs</li><li>Number of arguments must match number of parameters</li></ul></li></ol><h3>🚀 Coming Up Next:</h3><p>In the next lesson, you'll learn about <strong>variable scope</strong> - why variables inside functions are different from variables outside, and how Python decides which variable to use. Understanding scope prevents many common bugs!</p>"
    }
  ],
  "checkpoint_quiz": [
    {
      "question": "What is the difference between a parameter and an argument?",
      "options": [
        "There is no difference, they're the same thing",
        "Parameters are in the function definition, arguments are values passed when calling",
        "Parameters are for numbers, arguments are for strings",
        "Arguments are in the function definition, parameters are when calling"
      ],
      "correct_answer": 1,
      "explanation": "Parameters are variables in the function DEFINITION (def greet(name):), while arguments are the actual VALUES you pass when CALLING the function (greet('Alice')). Think: parameters are empty boxes in the recipe, arguments are the ingredients you put in."
    },
    {
      "question": "What does this function return?\n\ndef calculate(x):\n    result = x * 3\n    print(result)\n\nvalue = calculate(5)",
      "options": [
        "15",
        "None",
        "5",
        "Error"
      ],
      "correct_answer": 1,
      "explanation": "This function returns None! It prints 15 to the screen, but there's no 'return' statement, so Python automatically returns None. The variable 'value' will be None, not 15. To fix this, add 'return result' at the end."
    },
    {
      "question": "How do you return multiple values from a function?",
      "options": [
        "Use multiple return statements",
        "Separate values with commas: return value1, value2",
        "Put values in square brackets: return [value1, value2]",
        "You can't return multiple values"
      ],
      "correct_answer": 1,
      "explanation": "Use commas to return multiple values: 'return first, last'. Python automatically packs them into a tuple. You can then unpack when receiving: 'first_name, last_name = get_names()'. Option C (list) would work but returns a single list object, not multiple values."
    }
  ]
}
