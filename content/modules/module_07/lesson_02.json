{
  "lesson_id": 2,
  "module_id": 7,
  "title": "Dictionary Methods and Operations",
  "duration": "30 minutes",
  "objectives": [
    "Master essential dictionary methods like .get(), .update(), and .pop()",
    "Learn safe ways to access dictionary values without KeyError",
    "Understand how to iterate over keys, values, and key-value pairs",
    "Practice merging, copying, and clearing dictionaries"
  ],
  "sections": [
    {
      "role": "The Simplifier",
      "title": "The Concept",
      "content": "<h3>🔧 Dictionary Power Tools</h3><p><strong>Real-World Analogy First:</strong></p><p>Think of a filing cabinet in an office:</p><ul><li>📂 <strong>.get():</strong> \"Check if there's a file labeled 'Budget2024', but don't panic if it's missing - just tell me 'Not Found'\"</li><li>📂 <strong>.update():</strong> \"Take all these files from that other cabinet and add them to this one\"</li><li>📂 <strong>.pop():</strong> \"Remove the 'OldContract' file and hand it to me\"</li><li>📂 <strong>.keys():</strong> \"Show me all the labels on the files\"</li><li>📂 <strong>.values():</strong> \"Show me all the documents, without the labels\"</li><li>📂 <strong>.items():</strong> \"Show me each label WITH its document\"</li></ul><p>These are the methods that make dictionaries incredibly powerful and flexible!</p><hr><p><strong>Now The Technical Terms:</strong></p><p>Dictionary methods are built-in functions that perform common operations on dictionaries. They make your code safer, cleaner, and more efficient.</p><p><strong>Why Methods Matter:</strong></p><ul><li>✅ <strong>Safety:</strong> <code>.get()</code> prevents KeyError crashes</li><li>✅ <strong>Convenience:</strong> <code>.update()</code> merges dictionaries in one line</li><li>✅ <strong>Flexibility:</strong> <code>.pop()</code> removes and returns in one operation</li><li>✅ <strong>Iteration:</strong> <code>.keys()</code>, <code>.values()</code>, <code>.items()</code> make looping easy</li></ul>"
    },
    {
      "role": "The Coder",
      "title": "Code Example",
      "content": "<h3>💻 Essential Dictionary Methods</h3><p>Let's explore the most important dictionary methods:</p>",
      "code": "# ═══════════════════════════════════════\n# EXAMPLE 1: .get() - Safe Value Access\n# ═══════════════════════════════════════\n\nprint(\"Example 1: .get() method - safe access\")\nprint()\n\nperson = {\"name\": \"Alice\", \"age\": 25}\n\n# ❌ Direct access - crashes if key doesn't exist\ntry:\n    email = person[\"email\"]  # KeyError!\nexcept KeyError:\n    print(\"❌ Direct access caused KeyError for missing 'email' key\")\n\n# ✅ .get() method - returns None if key doesn't exist\nemail = person.get(\"email\")\nprint(f\"✅ .get() returned: {email} (no crash!)\")\n\n# ✅ .get() with default value\nemail = person.get(\"email\", \"no-email@example.com\")\nprint(f\"✅ .get() with default: {email}\")\n\n# Works for existing keys too\nname = person.get(\"name\")\nprint(f\"✅ .get() for existing key: {name}\")\nprint()\n\n# ═══════════════════════════════════════\n# EXAMPLE 2: .keys(), .values(), .items()\n# ═══════════════════════════════════════\n\nprint(\"Example 2: Accessing keys, values, and items\")\nprint()\n\nproduct = {\n    \"name\": \"Laptop\",\n    \"price\": 999.99,\n    \"brand\": \"TechCorp\",\n    \"stock\": 15\n}\n\n# Get all keys\nprint(\"All keys:\")\nfor key in product.keys():\n    print(f\"  {key}\")\nprint()\n\n# Get all values\nprint(\"All values:\")\nfor value in product.values():\n    print(f\"  {value}\")\nprint()\n\n# Get all key-value pairs\nprint(\"All key-value pairs:\")\nfor key, value in product.items():\n    print(f\"  {key}: {value}\")\nprint()\n\n# ═══════════════════════════════════════\n# EXAMPLE 3: .update() - Merge Dictionaries\n# ═══════════════════════════════════════\n\nprint(\"Example 3: .update() method - merging dictionaries\")\nprint()\n\nuser = {\"name\": \"Bob\", \"age\": 30}\nprint(f\"Original user: {user}\")\n\n# Update with new information\nnew_info = {\"email\": \"bob@example.com\", \"city\": \"NYC\"}\nuser.update(new_info)\nprint(f\"After update: {user}\")\n\n# Update also modifies existing keys\nchanges = {\"age\": 31, \"verified\": True}\nuser.update(changes)\nprint(f\"After second update: {user}\")\nprint()\n\n# ═══════════════════════════════════════\n# EXAMPLE 4: .pop() - Remove and Return\n# ═══════════════════════════════════════\n\nprint(\"Example 4: .pop() method - remove and return\")\nprint()\n\nconfig = {\n    \"theme\": \"dark\",\n    \"language\": \"en\",\n    \"notifications\": True,\n    \"beta_features\": False\n}\n\nprint(f\"Original config: {config}\")\n\n# Remove and get the value\nbeta_setting = config.pop(\"beta_features\")\nprint(f\"\\nRemoved beta_features: {beta_setting}\")\nprint(f\"Updated config: {config}\")\n\n# .pop() with default (won't crash if key missing)\nvolume = config.pop(\"volume\", 50)  # Key doesn't exist, returns 50\nprint(f\"\\nPopped 'volume' (doesn't exist): {volume}\")\nprint(f\"Config unchanged: {config}\")\nprint()\n\n# ═══════════════════════════════════════\n# EXAMPLE 5: .setdefault() - Get or Create\n# ═══════════════════════════════════════\n\nprint(\"Example 5: .setdefault() - get value or set default\")\nprint()\n\nuser_prefs = {\"theme\": \"light\"}\nprint(f\"Original preferences: {user_prefs}\")\n\n# Get existing value\ntheme = user_prefs.setdefault(\"theme\", \"dark\")\nprint(f\"Theme (exists): {theme}\")\nprint(f\"Prefs: {user_prefs}\")  # Unchanged\n\n# Set default for missing key\nlanguage = user_prefs.setdefault(\"language\", \"en\")\nprint(f\"\\nLanguage (new): {language}\")\nprint(f\"Prefs: {user_prefs}\")  # 'language' was added!\nprint()\n\n# ═══════════════════════════════════════\n# EXAMPLE 6: .copy() and .clear()\n# ═══════════════════════════════════════\n\nprint(\"Example 6: .copy() and .clear() methods\")\nprint()\n\noriginal = {\"a\": 1, \"b\": 2, \"c\": 3}\nprint(f\"Original: {original}\")\n\n# Create a copy (separate dictionary)\ncopy = original.copy()\nprint(f\"Copy: {copy}\")\n\n# Modify the copy\ncopy[\"a\"] = 99\nprint(f\"\\nAfter modifying copy:\")\nprint(f\"  Copy: {copy}\")\nprint(f\"  Original: {original} (unchanged!)\")\n\n# Clear all items\ncopy.clear()\nprint(f\"\\nAfter clearing copy: {copy}\")\nprint(f\"Original still intact: {original}\")\nprint()\n\n# ═══════════════════════════════════════\n# EXAMPLE 7: Membership Testing with 'in'\n# ═══════════════════════════════════════\n\nprint(\"Example 7: Checking if keys exist\")\nprint()\n\ninventory = {\n    \"apples\": 50,\n    \"bananas\": 30,\n    \"oranges\": 40\n}\n\n# Check if key exists\nif \"apples\" in inventory:\n    print(f\"✅ We have {inventory['apples']} apples in stock\")\n\nif \"grapes\" in inventory:\n    print(\"We have grapes\")\nelse:\n    print(\"❌ No grapes in inventory\")\n\n# Check multiple items\nneeded_items = [\"apples\", \"grapes\", \"bananas\"]\nprint(\"\\nChecking shopping list:\")\nfor item in needed_items:\n    if item in inventory:\n        print(f\"  ✅ {item}: {inventory[item]} available\")\n    else:\n        print(f\"  ❌ {item}: out of stock\")\nprint()\n\n# ═══════════════════════════════════════\n# EXAMPLE 8: Practical Use Case - Count Occurrences\n# ═══════════════════════════════════════\n\nprint(\"Example 8: Counting items with .get()\")\nprint()\n\nvotes = [\"Alice\", \"Bob\", \"Alice\", \"Carol\", \"Bob\", \"Alice\", \"Dave\", \"Bob\"]\nprint(f\"Votes: {votes}\")\nprint()\n\nvote_count = {}\nfor name in votes:\n    # Use .get() with default 0, then add 1\n    vote_count[name] = vote_count.get(name, 0) + 1\n\nprint(\"Vote count:\")\nfor person, count in vote_count.items():\n    print(f\"  {person}: {count} vote(s)\")\n\n# Find winner\nwinner = max(vote_count, key=vote_count.get)\nprint(f\"\\n🏆 Winner: {winner} with {vote_count[winner]} votes!\")",
      "output": "Example 1: .get() method - safe access\n\n❌ Direct access caused KeyError for missing 'email' key\n✅ .get() returned: None (no crash!)\n✅ .get() with default: no-email@example.com\n✅ .get() for existing key: Alice\n\nExample 2: Accessing keys, values, and items\n\nAll keys:\n  name\n  price\n  brand\n  stock\n\nAll values:\n  Laptop\n  999.99\n  TechCorp\n  15\n\nAll key-value pairs:\n  name: Laptop\n  price: 999.99\n  brand: TechCorp\n  stock: 15\n\nExample 3: .update() method - merging dictionaries\n\nOriginal user: {'name': 'Bob', 'age': 30}\nAfter update: {'name': 'Bob', 'age': 30, 'email': 'bob@example.com', 'city': 'NYC'}\nAfter second update: {'name': 'Bob', 'age': 31, 'email': 'bob@example.com', 'city': 'NYC', 'verified': True}\n\nExample 4: .pop() method - remove and return\n\nOriginal config: {'theme': 'dark', 'language': 'en', 'notifications': True, 'beta_features': False}\n\nRemoved beta_features: False\nUpdated config: {'theme': 'dark', 'language': 'en', 'notifications': True}\n\nPopped 'volume' (doesn't exist): 50\nConfig unchanged: {'theme': 'dark', 'language': 'en', 'notifications': True}\n\nExample 5: .setdefault() - get value or set default\n\nOriginal preferences: {'theme': 'light'}\nTheme (exists): light\nPrefs: {'theme': 'light'}\n\nLanguage (new): en\nPrefs: {'theme': 'light', 'language': 'en'}\n\nExample 6: .copy() and .clear() methods\n\nOriginal: {'a': 1, 'b': 2, 'c': 3}\nCopy: {'a': 1, 'b': 2, 'c': 3}\n\nAfter modifying copy:\n  Copy: {'a': 99, 'b': 2, 'c': 3}\n  Original: {'a': 1, 'b': 2, 'c': 3} (unchanged!)\n\nAfter clearing copy: {}\nOriginal still intact: {'a': 1, 'b': 2, 'c': 3}\n\nExample 7: Checking if keys exist\n\n✅ We have 50 apples in stock\n❌ No grapes in inventory\n\nChecking shopping list:\n  ✅ apples: 50 available\n  ❌ grapes: out of stock\n  ✅ bananas: 30 available\n\nExample 8: Counting items with .get()\n\nVotes: ['Alice', 'Bob', 'Alice', 'Carol', 'Bob', 'Alice', 'Dave', 'Bob']\n\nVote count:\n  Alice: 3 vote(s)\n  Bob: 3 vote(s)\n  Carol: 1 vote(s)\n  Dave: 1 vote(s)\n\n🏆 Winner: Alice with 3 votes!"
    },
    {
      "role": "The Simplifier",
      "title": "Syntax Breakdown",
      "content": "<h3>🔍 Understanding Dictionary Methods</h3><p><strong>Method 1: .get() - Safe Access</strong></p><pre><code>value = dictionary.get(key, default)\n                       ^^^  ^^^^^^^  \n                       key  optional default</code></pre><ul><li>📌 Returns value for key if it exists</li><li>📌 Returns <code>None</code> if key doesn't exist (no crash!)</li><li>📌 Returns <code>default</code> if provided and key doesn't exist</li><li>📌 NEVER raises KeyError</li></ul><pre><code># Comparison\nage = person[\"age\"]          # ❌ KeyError if not exists\nage = person.get(\"age\")      # ✅ Returns None if not exists\nage = person.get(\"age\", 0)   # ✅ Returns 0 if not exists</code></pre><hr><p><strong>Method 2: .keys() - Get All Keys</strong></p><pre><code>keys = dictionary.keys()</code></pre><ul><li>📌 Returns a view of all keys in the dictionary</li><li>📌 Can iterate over it: <code>for key in dict.keys():</code></li><li>📌 Convert to list: <code>list(dict.keys())</code></li><li>📌 Can check membership: <code>\"name\" in dict.keys()</code></li></ul><hr><p><strong>Method 3: .values() - Get All Values</strong></p><pre><code>values = dictionary.values()</code></pre><ul><li>📌 Returns a view of all values in the dictionary</li><li>📌 Can iterate: <code>for value in dict.values():</code></li><li>📌 Can use with functions: <code>max(dict.values())</code></li><li>📌 Values may include duplicates</li></ul><hr><p><strong>Method 4: .items() - Get All Key-Value Pairs</strong></p><pre><code>items = dictionary.items()\n\n# Most common use: iteration\nfor key, value in dictionary.items():\n    print(f\"{key}: {value}\")</code></pre><ul><li>📌 Returns tuples of (key, value) pairs</li><li>📌 Perfect for iterating over entire dictionary</li><li>📌 Can unpack in loop: <code>for k, v in dict.items():</code></li></ul><hr><p><strong>Method 5: .update() - Merge Dictionaries</strong></p><pre><code>dictionary.update(other_dictionary)\ndictionary.update({\"key\": \"value\"})  # Can use dict literal</code></pre><ul><li>📌 Adds all key-value pairs from other dictionary</li><li>📌 If key exists: overwrites with new value</li><li>📌 If key doesn't exist: adds it</li><li>📌 Modifies original dictionary (no return value)</li></ul><pre><code>user = {\"name\": \"Alice\", \"age\": 25}\nuser.update({\"age\": 26, \"city\": \"NYC\"})\n# Result: {\"name\": \"Alice\", \"age\": 26, \"city\": \"NYC\"}\n#         age updated ^^^      city added ^^^</code></pre><hr><p><strong>Method 6: .pop() - Remove and Return</strong></p><pre><code>value = dictionary.pop(key, default)\n                       ^^^  ^^^^^^^  \n                       key  optional default</code></pre><ul><li>📌 Removes the key from dictionary</li><li>📌 Returns the value that was removed</li><li>📌 If key doesn't exist and no default: raises KeyError</li><li>📌 If key doesn't exist and default provided: returns default (no remove)</li></ul><pre><code>config = {\"theme\": \"dark\", \"language\": \"en\"}\ntheme = config.pop(\"theme\")  # Returns \"dark\", removes from dict\n# config is now: {\"language\": \"en\"}\n\nvolume = config.pop(\"volume\", 50)  # Key doesn't exist, returns 50\n# config unchanged: {\"language\": \"en\"}</code></pre><hr><p><strong>Method 7: .setdefault() - Get or Initialize</strong></p><pre><code>value = dictionary.setdefault(key, default)</code></pre><ul><li>📌 If key exists: returns its value (like .get())</li><li>📌 If key doesn't exist: sets it to default AND returns default</li><li>📌 Useful for initializing nested structures</li></ul><pre><code># Pattern for counting\ncount = {}\nfor item in items:\n    count[item] = count.setdefault(item, 0) + 1\n    #             ^^^^^^^^^^^^^^^^^^^^^^^^^^  \n    #             Gets current count or sets to 0</code></pre><hr><p><strong>Method 8: .copy() - Create Shallow Copy</strong></p><pre><code>new_dict = dictionary.copy()</code></pre><ul><li>📌 Creates a new dictionary with same key-value pairs</li><li>📌 <strong>Shallow copy:</strong> copies references, not nested objects</li><li>📌 Changes to copy don't affect original</li><li>📌 Changes to original don't affect copy</li></ul><hr><p><strong>Method 9: .clear() - Remove All Items</strong></p><pre><code>dictionary.clear()</code></pre><ul><li>📌 Removes all key-value pairs</li><li>📌 Dictionary becomes empty <code>{}</code></li><li>📌 Modifies in place (no return value)</li></ul><hr><p><strong>Method 10: Membership Test with 'in'</strong></p><pre><code>if key in dictionary:\n    # Key exists\n\nif key not in dictionary:\n    # Key doesn't exist</code></pre><ul><li>📌 Checks if key exists in dictionary</li><li>📌 Returns True or False</li><li>📌 Very fast operation (O(1))</li><li>📌 Only checks keys, not values!</li></ul><hr><p><strong>Quick Reference Table:</strong></p><table><tr><th>Method</th><th>Purpose</th><th>Returns</th></tr><tr><td>.get(k, d)</td><td>Safe access</td><td>Value or default</td></tr><tr><td>.keys()</td><td>All keys</td><td>Key view</td></tr><tr><td>.values()</td><td>All values</td><td>Value view</td></tr><tr><td>.items()</td><td>All pairs</td><td>Item view</td></tr><tr><td>.update(d)</td><td>Merge dicts</td><td>None</td></tr><tr><td>.pop(k, d)</td><td>Remove & get</td><td>Value or default</td></tr><tr><td>.setdefault(k, d)</td><td>Get or create</td><td>Value</td></tr><tr><td>.copy()</td><td>Duplicate</td><td>New dict</td></tr><tr><td>.clear()</td><td>Empty dict</td><td>None</td></tr></table>"
    },
    {
      "role": "The Coder",
      "title": "Exercise",
      "content": "<h3>✏️ Your Turn: Inventory Management System</h3><p><strong>Challenge:</strong> Create an inventory system using dictionary methods to manage product stock levels.</p><p><strong>Starting Inventory:</strong></p><pre><code>inventory = {\n    \"apples\": 50,\n    \"bananas\": 30,\n    \"oranges\": 40\n}</code></pre><p><strong>Requirements:</strong></p><ol><li><strong>Add new products:</strong> Add \"grapes\" with quantity 25 and \"mangoes\" with 15 using a single <code>.update()</code> call</li><li><strong>Sale transaction:</strong> Someone buys 10 apples. Use <code>.get()</code> to safely decrease the count. If the item doesn't exist, print an error message.</li><li><strong>Check product availability:</strong> Use <code>in</code> keyword to check if \"pineapple\" is in inventory. If not, print a message.</li><li><strong>Remove discontinued item:</strong> Use <code>.pop()</code> to remove \"oranges\" from inventory and print how many were removed.</li><li><strong>Stock report:</strong> Use <code>.items()</code> to print all products with their quantities in a formatted way.</li><li><strong>Low stock alert:</strong> Use <code>.items()</code> to find and print items with quantity less than 20.</li><li><strong>Total items in stock:</strong> Use <code>.values()</code> to calculate the total number of items across all products.</li></ol><p><strong>Bonus:</strong> Create a function <code>restock(product, amount)</code> that uses <code>.setdefault()</code> to add products if they don't exist or increase stock if they do.</p>",
      "starter_code": "# Starting inventory\ninventory = {\n    \"apples\": 50,\n    \"bananas\": 30,\n    \"oranges\": 40\n}\n\nprint(\"📦 INVENTORY MANAGEMENT SYSTEM\")\nprint(\"═\" * 50)\nprint(f\"Starting inventory: {inventory}\")\nprint()\n\n# Task 1: Add new products using .update()\nprint(\"Task 1: Adding new products...\")\n# Your code here\n\n# Task 2: Process a sale (10 apples)\nprint(\"\\nTask 2: Processing sale - 10 apples...\")\n# Your code here\n\n# Task 3: Check if pineapple is available\nprint(\"\\nTask 3: Checking for pineapple...\")\n# Your code here\n\n# Task 4: Remove oranges using .pop()\nprint(\"\\nTask 4: Discontinuing oranges...\")\n# Your code here\n\n# Task 5: Print stock report using .items()\nprint(\"\\nTask 5: Stock Report\")\nprint(\"─\" * 50)\n# Your code here\n\n# Task 6: Low stock alert (< 20 items)\nprint(\"\\nTask 6: Low Stock Alert\")\nprint(\"─\" * 50)\n# Your code here\n\n# Task 7: Calculate total items\nprint(\"\\nTask 7: Total Inventory\")\nprint(\"─\" * 50)\n# Your code here\n\n# BONUS: Restock function\nprint(\"\\nBONUS: Testing restock function\")\nprint(\"─\" * 50)\n\ndef restock(product, amount):\n    # Your code here\n    pass\n\n# Test the function\n# Your test code here\n"
    },
    {
      "role": "The Coder",
      "title": "Solution",
      "content": "<h3>✅ Solution: Inventory Management System</h3>",
      "code": "# Starting inventory\ninventory = {\n    \"apples\": 50,\n    \"bananas\": 30,\n    \"oranges\": 40\n}\n\nprint(\"📦 INVENTORY MANAGEMENT SYSTEM\")\nprint(\"═\" * 50)\nprint(f\"Starting inventory: {inventory}\")\nprint()\n\n# ═══════════════════════════════════════\n# Task 1: Add new products using .update()\n# ═══════════════════════════════════════\nprint(\"Task 1: Adding new products...\")\nnew_products = {\"grapes\": 25, \"mangoes\": 15}\ninventory.update(new_products)\nprint(f\"Added {list(new_products.keys())}\")\nprint(f\"Updated inventory: {inventory}\")\n\n# ═══════════════════════════════════════\n# Task 2: Process a sale (10 apples)\n# ═══════════════════════════════════════\nprint(\"\\nTask 2: Processing sale - 10 apples...\")\nproduct = \"apples\"\nquantity_sold = 10\n\n# Use .get() to safely access current stock\ncurrent_stock = inventory.get(product)\n\nif current_stock is not None:\n    if current_stock >= quantity_sold:\n        inventory[product] = current_stock - quantity_sold\n        print(f\"✅ Sold {quantity_sold} {product}\")\n        print(f\"   Remaining: {inventory[product]}\")\n    else:\n        print(f\"❌ Not enough {product} in stock!\")\n        print(f\"   Available: {current_stock}, Requested: {quantity_sold}\")\nelse:\n    print(f\"❌ Product '{product}' not found in inventory!\")\n\n# ═══════════════════════════════════════\n# Task 3: Check if pineapple is available\n# ═══════════════════════════════════════\nprint(\"\\nTask 3: Checking for pineapple...\")\nif \"pineapple\" in inventory:\n    print(f\"✅ Pineapple is available: {inventory['pineapple']} in stock\")\nelse:\n    print(\"❌ Pineapple is not in our inventory\")\n\n# ═══════════════════════════════════════\n# Task 4: Remove oranges using .pop()\n# ═══════════════════════════════════════\nprint(\"\\nTask 4: Discontinuing oranges...\")\noranges_removed = inventory.pop(\"oranges\", 0)\nprint(f\"Removed {oranges_removed} oranges from inventory\")\nprint(f\"Updated inventory: {inventory}\")\n\n# ═══════════════════════════════════════\n# Task 5: Print stock report using .items()\n# ═══════════════════════════════════════\nprint(\"\\nTask 5: Stock Report\")\nprint(\"─\" * 50)\nprint(f\"{'Product':<15} {'Quantity':>10}\")\nprint(\"─\" * 50)\nfor product, quantity in inventory.items():\n    print(f\"{product.capitalize():<15} {quantity:>10}\")\nprint(\"─\" * 50)\n\n# ═══════════════════════════════════════\n# Task 6: Low stock alert (< 20 items)\n# ═══════════════════════════════════════\nprint(\"\\nTask 6: Low Stock Alert\")\nprint(\"─\" * 50)\nlow_stock_items = []\nfor product, quantity in inventory.items():\n    if quantity < 20:\n        low_stock_items.append((product, quantity))\n        print(f\"⚠️  {product.capitalize()}: Only {quantity} remaining!\")\n\nif not low_stock_items:\n    print(\"✅ All items are well stocked\")\nelse:\n    print(f\"\\nTotal low stock items: {len(low_stock_items)}\")\n\n# ═══════════════════════════════════════\n# Task 7: Calculate total items\n# ═══════════════════════════════════════\nprint(\"\\nTask 7: Total Inventory\")\nprint(\"─\" * 50)\ntotal_items = sum(inventory.values())\nproduct_count = len(inventory)\naverage_stock = total_items / product_count if product_count > 0 else 0\n\nprint(f\"Total products: {product_count}\")\nprint(f\"Total items in stock: {total_items}\")\nprint(f\"Average stock per product: {average_stock:.1f}\")\n\n# ═══════════════════════════════════════\n# BONUS: Restock function\n# ═══════════════════════════════════════\nprint(\"\\nBONUS: Testing restock function\")\nprint(\"─\" * 50)\n\ndef restock(product, amount):\n    \"\"\"Restock a product, adding it if it doesn't exist.\n    \n    Args:\n        product: Product name\n        amount: Quantity to add\n    \n    Returns:\n        New total quantity\n    \"\"\"\n    # Use .setdefault() to get current value or initialize to 0\n    current = inventory.setdefault(product, 0)\n    new_total = current + amount\n    inventory[product] = new_total\n    \n    if current == 0:\n        print(f\"  ➕ Added new product '{product}': {amount} units\")\n    else:\n        print(f\"  📦 Restocked '{product}': {current} + {amount} = {new_total}\")\n    \n    return new_total\n\n# Test the restock function\nprint(\"\\nRestocking operations:\")\nrestock(\"apples\", 20)      # Existing product\nrestock(\"pineapple\", 30)   # New product\nrestock(\"bananas\", 15)     # Existing product\n\nprint(\"\\n\" + \"═\" * 50)\nprint(\"FINAL INVENTORY\")\nprint(\"═\" * 50)\nfor product, quantity in sorted(inventory.items()):\n    status = \"⚠️ LOW\" if quantity < 20 else \"✅\"\n    print(f\"{status} {product.capitalize():<15} {quantity:>10}\")\n\nprint(\"═\" * 50)\nprint(f\"Grand Total: {sum(inventory.values())} items across {len(inventory)} products\")\nprint(\"═\" * 50)",
      "output": "📦 INVENTORY MANAGEMENT SYSTEM\n══════════════════════════════════════════════════\nStarting inventory: {'apples': 50, 'bananas': 30, 'oranges': 40}\n\nTask 1: Adding new products...\nAdded ['grapes', 'mangoes']\nUpdated inventory: {'apples': 50, 'bananas': 30, 'oranges': 40, 'grapes': 25, 'mangoes': 15}\n\nTask 2: Processing sale - 10 apples...\n✅ Sold 10 apples\n   Remaining: 40\n\nTask 3: Checking for pineapple...\n❌ Pineapple is not in our inventory\n\nTask 4: Discontinuing oranges...\nRemoved 40 oranges from inventory\nUpdated inventory: {'apples': 40, 'bananas': 30, 'grapes': 25, 'mangoes': 15}\n\nTask 5: Stock Report\n──────────────────────────────────────────────────\nProduct              Quantity\n──────────────────────────────────────────────────\nApples                     40\nBananas                    30\nGrapes                     25\nMangoes                    15\n──────────────────────────────────────────────────\n\nTask 6: Low Stock Alert\n──────────────────────────────────────────────────\n⚠️  Mangoes: Only 15 remaining!\n\nTotal low stock items: 1\n\nTask 7: Total Inventory\n──────────────────────────────────────────────────\nTotal products: 4\nTotal items in stock: 110\nAverage stock per product: 27.5\n\nBONUS: Testing restock function\n──────────────────────────────────────────────────\n\nRestocking operations:\n  📦 Restocked 'apples': 40 + 20 = 60\n  ➕ Added new product 'pineapple': 30 units\n  📦 Restocked 'bananas': 30 + 15 = 45\n\n══════════════════════════════════════════════════\nFINAL INVENTORY\n══════════════════════════════════════════════════\n✅ Apples                     60\n✅ Bananas                    45\n✅ Grapes                     25\n⚠️ LOW Mangoes                    15\n✅ Pineapple                  30\n══════════════════════════════════════════════════\nGrand Total: 175 items across 5 products\n══════════════════════════════════════════════════",
      "explanation": "<h3>📚 How Each Part Works:</h3><ol><li><strong>Task 1 - .update() for Bulk Adding (Lines 16-19):</strong><pre><code>new_products = {\"grapes\": 25, \"mangoes\": 15}\ninventory.update(new_products)</code></pre><ul><li>Create a dictionary with new products</li><li><code>.update()</code> adds all key-value pairs at once</li><li>More efficient than adding one by one</li></ul></li><li><strong>Task 2 - Safe Access with .get() (Lines 26-38):</strong><pre><code>current_stock = inventory.get(product)\nif current_stock is not None:</code></pre><ul><li><code>.get()</code> returns None if key doesn't exist (no crash!)</li><li>Check if stock exists before processing</li><li>Can also check if enough stock before sale</li><li>Update the value after validation</li></ul></li><li><strong>Task 3 - Membership Check with 'in' (Lines 44-48):</strong><pre><code>if \"pineapple\" in inventory:</code></pre><ul><li>Fast way to check if key exists</li><li>Returns True or False</li><li>Clearer than try/except for this use case</li></ul></li><li><strong>Task 4 - Remove with .pop() (Lines 54-57):</strong><pre><code>oranges_removed = inventory.pop(\"oranges\", 0)</code></pre><ul><li><code>.pop()</code> removes AND returns the value</li><li>Second argument (0) is default if key doesn't exist</li><li>Two operations in one: remove + get value</li></ul></li><li><strong>Task 5 - Iterate with .items() (Lines 63-68):</strong><pre><code>for product, quantity in inventory.items():</code></pre><ul><li><code>.items()</code> returns (key, value) pairs</li><li>Unpack into two variables in the loop</li><li>Most common way to loop over dictionaries</li></ul></li><li><strong>Task 6 - Filter Using .items() (Lines 74-82):</strong><pre><code>for product, quantity in inventory.items():\n    if quantity < 20:</code></pre><ul><li>Loop through all items</li><li>Filter by checking a condition on values</li><li>Collect items that match criteria</li></ul></li><li><strong>Task 7 - Sum with .values() (Line 89):</strong><pre><code>total_items = sum(inventory.values())</code></pre><ul><li><code>.values()</code> returns all values</li><li><code>sum()</code> adds them up</li><li>Clean, one-line calculation</li></ul></li><li><strong>BONUS - .setdefault() Pattern (Lines 101-113):</strong><pre><code>current = inventory.setdefault(product, 0)\nnew_total = current + amount\ninventory[product] = new_total</code></pre><ul><li><code>.setdefault()</code> gets value OR creates with default</li><li>If product exists: returns current stock</li><li>If product doesn't exist: adds it with value 0</li><li>Then add the restock amount</li></ul></li></ol><h3>⚠️ Common Mistakes:</h3><ol><li><strong>Not Checking if .get() Returned None:</strong><pre><code># ❌ WRONG - will crash if key doesn't exist\nstock = inventory.get(\"apples\")\nstock = stock - 10  # TypeError if stock is None!\n\n# ✅ CORRECT - check first\nstock = inventory.get(\"apples\")\nif stock is not None:\n    stock = stock - 10</code></pre></li><li><strong>Forgetting Default in .pop():</strong><pre><code># ❌ WRONG - KeyError if key doesn't exist\nvalue = inventory.pop(\"missing_item\")\n\n# ✅ CORRECT - provide default\nvalue = inventory.pop(\"missing_item\", 0)</code></pre></li><li><strong>Modifying Dict While Iterating:</strong><pre><code># ❌ WRONG - can cause RuntimeError\nfor product in inventory:\n    if inventory[product] < 10:\n        inventory.pop(product)  # Modifying while iterating!\n\n# ✅ CORRECT - iterate over copy\nfor product in list(inventory.keys()):\n    if inventory[product] < 10:\n        inventory.pop(product)</code></pre></li><li><strong>Confusing .update() and Assignment:</strong><pre><code># .update() MODIFIES the original\noriginal = {\"a\": 1}\noriginal.update({\"b\": 2})\n# original is now {\"a\": 1, \"b\": 2}\n\n# Assignment REPLACES\noriginal = {\"a\": 1}\noriginal = {\"b\": 2}\n# original is now {\"b\": 2} only!</code></pre></li></ol><h3>💡 Pro Tips:</h3><ul><li>✅ <strong>Use .get() with defaults:</strong> <code>count = scores.get(player, 0)</code> prevents crashes</li><li>✅ <strong>.items() for iteration:</strong> Most readable way to loop over dictionaries</li><li>✅ <strong>.pop() with default:</strong> Safe removal that won't crash</li><li>✅ <strong>.update() for merging:</strong> Clean way to combine dictionaries</li><li>✅ <strong>'in' for existence checks:</strong> Faster and clearer than try/except</li><li>✅ <strong>.setdefault() for initialization:</strong> Perfect for counters and accumulators</li></ul>"
    },
    {
      "role": "Summary",
      "title": "Key Takeaways",
      "content": "<h3>🎯 Essential Points to Remember:</h3><ol><li><strong>.get() - Safe value access (prevents KeyError):</strong><pre><code>value = dict.get(key, default)\n# Returns value if key exists, default otherwise</code></pre><ul><li>NEVER raises KeyError</li><li>Returns None if no default specified</li><li>Returns default if key doesn't exist</li></ul></li><li><strong>.keys(), .values(), .items() - Access dict contents:</strong><pre><code>dict.keys()    # All keys\ndict.values()  # All values\ndict.items()   # All (key, value) pairs</code></pre><ul><li>Return views (not lists, but can iterate)</li><li>Use <code>.items()</code> for looping: <code>for k, v in dict.items():</code></li><li>Convert to list if needed: <code>list(dict.keys())</code></li></ul></li><li><strong>.update() - Merge dictionaries:</strong><pre><code>dict1.update(dict2)\n# Adds all items from dict2 to dict1\n# Overwrites existing keys, adds new ones</code></pre><ul><li>Modifies the original dictionary</li><li>Returns None (modifies in place)</li></ul></li><li><strong>.pop() - Remove and return:</strong><pre><code>value = dict.pop(key, default)\n# Removes key and returns its value</code></pre><ul><li>Removes the key-value pair</li><li>Returns the value</li><li>Provide default to prevent KeyError</li></ul></li><li><strong>.setdefault() - Get or initialize:</strong><pre><code>value = dict.setdefault(key, default)\n# If key exists: returns value\n# If not: sets key=default and returns default</code></pre><ul><li>Useful for initializing nested structures</li><li>Common in counting patterns</li></ul></li><li><strong>.copy() - Duplicate dictionary:</strong><pre><code>new_dict = original.copy()\n# Creates separate dictionary</code></pre><ul><li>Changes to copy don't affect original</li><li>Shallow copy (nested objects are referenced)</li></ul></li><li><strong>.clear() - Remove all items:</strong><pre><code>dict.clear()\n# Dictionary becomes empty {}</code></pre></li><li><strong>'in' keyword - Check if key exists:</strong><pre><code>if key in dict:\n    # Key exists\nif key not in dict:\n    # Key doesn't exist</code></pre><ul><li>Fast operation (O(1))</li><li>Checks keys only, not values</li></ul></li><li><strong>Method comparison table:</strong><table><tr><th>When you need to...</th><th>Use this</th></tr><tr><td>Get value safely</td><td>.get(key, default)</td></tr><tr><td>Loop over all items</td><td>.items()</td></tr><tr><td>Merge dictionaries</td><td>.update(other)</td></tr><tr><td>Remove and get value</td><td>.pop(key, default)</td></tr><tr><td>Check if key exists</td><td>key in dict</td></tr><tr><td>Initialize if missing</td><td>.setdefault(key, default)</td></tr><tr><td>Make a copy</td><td>.copy()</td></tr></table></li><li><strong>Common patterns:</strong><pre><code># Counting pattern\nfor item in items:\n    count[item] = count.get(item, 0) + 1\n\n# Default initialization\ndata[key] = data.setdefault(key, [])\ndata[key].append(value)\n\n# Safe removal\nremoved = dict.pop('key', None)\nif removed:\n    print(f\"Removed {removed}\")</code></pre></li></ol><h3>🚀 Coming Up Next:</h3><p>In the next lesson, you'll learn about <strong>Nested Dictionaries</strong> - how to store complex, hierarchical data like databases of students with courses and grades, or JSON-like structures!</p>"
    }
  ],
  "checkpoint_quiz": [
    {
      "question": "What does dict.get('key', 'default') return if 'key' doesn't exist?",
      "options": [
        "Raises KeyError",
        "Returns None",
        "Returns 'default'",
        "Returns an empty string"
      ],
      "correct_answer": 2,
      "explanation": "dict.get('key', 'default') returns 'default' if the key doesn't exist. This is the whole point of .get() - it provides a safe way to access values with a fallback. Without the second argument, it would return None. It never raises KeyError."
    },
    {
      "question": "Which method should you use to iterate over both keys AND values in a dictionary?",
      "options": [
        ".keys()",
        ".values()",
        ".items()",
        ".pairs()"
      ],
      "correct_answer": 2,
      "explanation": ".items() returns (key, value) pairs, perfect for iteration: 'for key, value in dict.items()'. .keys() only gives keys, .values() only gives values, and .pairs() doesn't exist. .items() is the most common way to loop over dictionaries."
    },
    {
      "question": "What does dict.pop('key', 0) do if 'key' exists in the dictionary?",
      "options": [
        "Returns 0",
        "Removes the key and returns its value",
        "Returns the value but doesn't remove the key",
        "Raises KeyError"
      ],
      "correct_answer": 1,
      "explanation": ".pop('key', 0) REMOVES the key from the dictionary AND RETURNS its value (if the key exists). The 0 is only used as a default if the key doesn't exist. So if key exists, it's removed and its value is returned. If it doesn't exist, it returns 0 without error."
    }
  ]
}
