{
  "lesson_id": 6,
  "module_id": 7,
  "title": "Mini-Project: Contact Manager System",
  "duration": "45 minutes",
  "objectives": [
    "Apply all dictionary and set concepts in a real-world project",
    "Build a complete contact management system",
    "Practice data validation and user interaction",
    "Combine dictionaries, sets, and comprehensions effectively"
  ],
  "sections": [
    {
      "role": "The Simplifier",
      "title": "The Concept",
      "content": "<h3>📇 Building a Contact Manager</h3><p><strong>Real-World Application:</strong></p><p>A contact manager is like a digital address book where you can:</p><ul><li>📝 Store contact information (name, phone, email, tags)</li><li>🔍 Search for contacts by various criteria</li><li>🏷️ Tag contacts with categories (work, family, friends)</li><li>📊 Analyze your contact database</li></ul><p>This project combines everything you've learned about dictionaries and sets!</p><p><strong>Skills You'll Practice:</strong></p><ul><li>✅ Nested dictionaries (contact details)</li><li>✅ Dictionary methods (.get(), .update(), etc.)</li><li>✅ Sets (unique tags, email domains)</li><li>✅ Comprehensions (filtering, transforming)</li><li>✅ Data validation</li><li>✅ Menu-driven program structure</li></ul>"
    },
    {
      "role": "The Coder",
      "title": "Code Example",
      "content": "<h3>💻 Complete Contact Manager System</h3>",
      "code": "\"\"\"\nCONTACT MANAGER SYSTEM\nA comprehensive contact management application using dictionaries and sets.\n\"\"\"\n\n# Contact database - dictionary of dictionaries\ncontacts = {\n    \"alice_j\": {\n        \"name\": \"Alice Johnson\",\n        \"phone\": \"555-0101\",\n        \"email\": \"alice@techcorp.com\",\n        \"tags\": {\"work\", \"developer\", \"friend\"}\n    },\n    \"bob_s\": {\n        \"name\": \"Bob Smith\",\n        \"phone\": \"555-0102\",\n        \"email\": \"bob@email.com\",\n        \"tags\": {\"family\", \"brother\"}\n    },\n    \"carol_d\": {\n        \"name\": \"Carol Davis\",\n        \"phone\": \"555-0103\",\n        \"email\": \"carol@techcorp.com\",\n        \"tags\": {\"work\", \"manager\"}\n    }\n}\n\ndef add_contact(contact_id, name, phone, email, tags=None):\n    \"\"\"Add a new contact to the database.\"\"\"\n    if contact_id in contacts:\n        return False, \"Contact ID already exists\"\n    \n    contacts[contact_id] = {\n        \"name\": name,\n        \"phone\": phone,\n        \"email\": email,\n        \"tags\": set(tags) if tags else set()\n    }\n    return True, \"Contact added successfully\"\n\ndef get_contact(contact_id):\n    \"\"\"Retrieve a contact by ID.\"\"\"\n    return contacts.get(contact_id)\n\ndef update_contact(contact_id, **updates):\n    \"\"\"Update contact information.\"\"\"\n    if contact_id not in contacts:\n        return False, \"Contact not found\"\n    \n    contact = contacts[contact_id]\n    \n    # Update allowed fields\n    for field in ['name', 'phone', 'email']:\n        if field in updates:\n            contact[field] = updates[field]\n    \n    # Handle tags specially (set operations)\n    if 'add_tags' in updates:\n        contact['tags'].update(updates['add_tags'])\n    if 'remove_tags' in updates:\n        contact['tags'].difference_update(updates['remove_tags'])\n    \n    return True, \"Contact updated successfully\"\n\ndef delete_contact(contact_id):\n    \"\"\"Remove a contact from the database.\"\"\"\n    if contact_id in contacts:\n        contacts.pop(contact_id)\n        return True, \"Contact deleted successfully\"\n    return False, \"Contact not found\"\n\ndef search_by_tag(tag):\n    \"\"\"Find all contacts with a specific tag.\"\"\"\n    return {cid: contact for cid, contact in contacts.items() \n            if tag in contact['tags']}\n\ndef search_by_name(query):\n    \"\"\"Find contacts whose name contains the query string.\"\"\"\n    query_lower = query.lower()\n    return {cid: contact for cid, contact in contacts.items() \n            if query_lower in contact['name'].lower()}\n\ndef get_all_tags():\n    \"\"\"Get set of all unique tags used.\"\"\"\n    all_tags = set()\n    for contact in contacts.values():\n        all_tags.update(contact['tags'])\n    return all_tags\n\ndef get_email_domains():\n    \"\"\"Get set of all unique email domains.\"\"\"\n    return {contact['email'].split('@')[1] for contact in contacts.values()}\n\ndef count_by_tag():\n    \"\"\"Count how many contacts have each tag.\"\"\"\n    tag_counts = {}\n    for contact in contacts.values():\n        for tag in contact['tags']:\n            tag_counts[tag] = tag_counts.get(tag, 0) + 1\n    return tag_counts\n\ndef display_contact(contact_id, contact):\n    \"\"\"Pretty print a contact.\"\"\"\n    print(f\"\\nID: {contact_id}\")\n    print(f\"Name: {contact['name']}\")\n    print(f\"Phone: {contact['phone']}\")\n    print(f\"Email: {contact['email']}\")\n    print(f\"Tags: {', '.join(sorted(contact['tags'])) if contact['tags'] else 'None'}\")\n\ndef display_statistics():\n    \"\"\"Show database statistics.\"\"\"\n    print(\"\\n📊 DATABASE STATISTICS\")\n    print(\"═\" * 50)\n    print(f\"Total contacts: {len(contacts)}\")\n    print(f\"\\nUnique tags: {len(get_all_tags())}\")\n    print(f\"Tags: {', '.join(sorted(get_all_tags()))}\")\n    print(f\"\\nEmail domains: {', '.join(sorted(get_email_domains()))}\")\n    \n    print(\"\\nContacts per tag:\")\n    tag_counts = count_by_tag()\n    for tag, count in sorted(tag_counts.items()):\n        print(f\"  {tag}: {count}\")\n\n# ═══════════════════════════════════════════════════\n# DEMONSTRATION\n# ═══════════════════════════════════════════════════\n\nprint(\"📇 CONTACT MANAGER SYSTEM\")\nprint(\"═\" * 60)\n\nprint(\"\\n1️⃣ INITIAL CONTACTS\")\nprint(\"─\" * 60)\nfor cid, contact in contacts.items():\n    display_contact(cid, contact)\n\nprint(\"\\n\\n2️⃣ ADDING NEW CONTACT\")\nprint(\"─\" * 60)\nsuccess, msg = add_contact(\n    \"dave_w\",\n    \"Dave Wilson\",\n    \"555-0104\",\n    \"dave@email.com\",\n    {\"friend\", \"gaming\"}\n)\nprint(f\"Result: {msg}\")\ndisplay_contact(\"dave_w\", contacts[\"dave_w\"])\n\nprint(\"\\n\\n3️⃣ UPDATING CONTACT\")\nprint(\"─\" * 60)\nprint(\"Adding tags to Alice...\")\nsuccess, msg = update_contact(\n    \"alice_j\",\n    add_tags={\"python\", \"mentor\"}\n)\nprint(f\"Result: {msg}\")\ndisplay_contact(\"alice_j\", contacts[\"alice_j\"])\n\nprint(\"\\n\\n4️⃣ SEARCHING BY TAG\")\nprint(\"─\" * 60)\nwork_contacts = search_by_tag(\"work\")\nprint(f\"Contacts tagged 'work': {len(work_contacts)}\")\nfor cid, contact in work_contacts.items():\n    print(f\"  • {contact['name']} ({contact['email']})\")\n\nprint(\"\\n\\n5️⃣ SEARCHING BY NAME\")\nprint(\"─\" * 60)\nresults = search_by_name(\"bob\")\nprint(f\"Contacts matching 'bob': {len(results)}\")\nfor cid, contact in results.items():\n    display_contact(cid, contact)\n\nprint(\"\\n\\n6️⃣ FINDING COMMON TAGS\")\nprint(\"─\" * 60)\nalice_tags = contacts[\"alice_j\"][\"tags\"]\ncarol_tags = contacts[\"carol_d\"][\"tags\"]\ncommon = alice_tags & carol_tags\nprint(f\"Alice's tags: {alice_tags}\")\nprint(f\"Carol's tags: {carol_tags}\")\nprint(f\"Common tags: {common}\")\n\nprint(\"\\n\\n7️⃣ EMAIL DOMAIN ANALYSIS\")\nprint(\"─\" * 60)\ndomains = get_email_domains()\nprint(f\"Email domains in use: {domains}\")\ntechcorp_contacts = {cid: c for cid, c in contacts.items() \n                     if '@techcorp.com' in c['email']}\nprint(f\"\\nContacts at techcorp.com: {len(techcorp_contacts)}\")\nfor cid, contact in techcorp_contacts.items():\n    print(f\"  • {contact['name']}\")\n\ndisplay_statistics()\n\nprint(\"\\n\\n8️⃣ ADVANCED QUERIES\")\nprint(\"─\" * 60)\n\n# Find contacts with multiple tags\nmulti_tagged = {cid: c for cid, c in contacts.items() if len(c['tags']) > 2}\nprint(f\"Contacts with 3+ tags: {len(multi_tagged)}\")\nfor cid, contact in multi_tagged.items():\n    print(f\"  • {contact['name']}: {len(contact['tags'])} tags\")\n\n# Find contacts without certain tags\nno_work = {cid: c for cid, c in contacts.items() if 'work' not in c['tags']}\nprint(f\"\\nNon-work contacts: {len(no_work)}\")\nfor cid, contact in no_work.items():\n    print(f\"  • {contact['name']}\")\n\nprint(\"\\n\" + \"═\" * 60)\nprint(\"✅ Contact Manager demonstration complete!\")\nprint(\"═\" * 60)",
      "output": "📇 CONTACT MANAGER SYSTEM\n════════════════════════════════════════════════════════════\n\n1️⃣ INITIAL CONTACTS\n────────────────────────────────────────────────────────────\n\nID: alice_j\nName: Alice Johnson\nPhone: 555-0101\nEmail: alice@techcorp.com\nTags: developer, friend, work\n\nID: bob_s\nName: Bob Smith\nPhone: 555-0102\nEmail: bob@email.com\nTags: brother, family\n\nID: carol_d\nName: Carol Davis\nPhone: 555-0103\nEmail: carol@techcorp.com\nTags: manager, work\n\n\n2️⃣ ADDING NEW CONTACT\n────────────────────────────────────────────────────────────\nResult: Contact added successfully\n\nID: dave_w\nName: Dave Wilson\nPhone: 555-0104\nEmail: dave@email.com\nTags: friend, gaming\n\n\n3️⃣ UPDATING CONTACT\n────────────────────────────────────────────────────────────\nAdding tags to Alice...\nResult: Contact updated successfully\n\nID: alice_j\nName: Alice Johnson\nPhone: 555-0101\nEmail: alice@techcorp.com\nTags: developer, friend, mentor, python, work\n\n\n4️⃣ SEARCHING BY TAG\n────────────────────────────────────────────────────────────\nContacts tagged 'work': 2\n  • Alice Johnson (alice@techcorp.com)\n  • Carol Davis (carol@techcorp.com)\n\n\n5️⃣ SEARCHING BY NAME\n────────────────────────────────────────────────────────────\nContacts matching 'bob': 1\n\nID: bob_s\nName: Bob Smith\nPhone: 555-0102\nEmail: bob@email.com\nTags: brother, family\n\n\n6️⃣ FINDING COMMON TAGS\n────────────────────────────────────────────────────────────\nAlice's tags: {'developer', 'friend', 'mentor', 'python', 'work'}\nCarol's tags: {'manager', 'work'}\nCommon tags: {'work'}\n\n\n7️⃣ EMAIL DOMAIN ANALYSIS\n────────────────────────────────────────────────────────────\nEmail domains in use: {'email.com', 'techcorp.com'}\n\nContacts at techcorp.com: 2\n  • Alice Johnson\n  • Carol Davis\n\n📊 DATABASE STATISTICS\n══════════════════════════════════════════════════════════\nTotal contacts: 4\n\nUnique tags: 9\nTags: brother, developer, family, friend, gaming, manager, mentor, python, work\n\nEmail domains: email.com, techcorp.com\n\nContacts per tag:\n  brother: 1\n  developer: 1\n  family: 1\n  friend: 2\n  gaming: 1\n  manager: 1\n  mentor: 1\n  python: 1\n  work: 2\n\n\n8️⃣ ADVANCED QUERIES\n────────────────────────────────────────────────────────────\nContacts with 3+ tags: 1\n  • Alice Johnson: 5 tags\n\nNon-work contacts: 2\n  • Bob Smith\n  • Dave Wilson\n\n════════════════════════════════════════════════════════════\n✅ Contact Manager demonstration complete!\n════════════════════════════════════════════════════════════"
    },
    {
      "role": "The Simplifier",
      "title": "Syntax Breakdown",
      "content": "<h3>🔍 Key Patterns Used</h3><p><strong>1. Nested Dictionary Structure:</strong></p><pre><code>contacts = {\n    \"contact_id\": {           # Outer dict: ID → Contact\n        \"name\": \"...\",        # Inner dict: field → value\n        \"phone\": \"...\",\n        \"email\": \"...\",\n        \"tags\": {...}         # Set of tags\n    }\n}</code></pre><p><strong>2. Safe Dictionary Access:</strong></p><pre><code># Using .get() to avoid KeyError\ncontact = contacts.get(contact_id)\nif contact:\n    # Process contact\nelse:\n    # Handle not found</code></pre><p><strong>3. Dictionary Comprehension for Filtering:</strong></p><pre><code># Find all contacts with specific tag\nwork_contacts = {cid: contact for cid, contact in contacts.items() \n                 if tag in contact['tags']}</code></pre><p><strong>4. Set Operations on Tags:</strong></p><pre><code># Add tags\ncontact['tags'].update(new_tags)\n\n# Remove tags\ncontact['tags'].difference_update(old_tags)\n\n# Find common tags\ncommon = set1 & set2</code></pre><p><strong>5. Set Comprehension for Unique Values:</strong></p><pre><code># Get all unique email domains\ndomains = {c['email'].split('@')[1] for c in contacts.values()}</code></pre>"
    },
    {
      "role": "The Coder",
      "title": "Exercise",
      "content": "<h3>✏️ Your Turn: Extend the Contact Manager</h3><p><strong>Challenge:</strong> Add new features to the contact manager.</p><p><strong>Requirements:</strong></p><ol><li><strong>Add a function <code>export_by_tag(tag)</code></strong> that returns a list of contact names with that tag, sorted alphabetically</li><li><strong>Add a function <code>find_duplicates()</code></strong> that finds contacts with the same phone number or email</li><li><strong>Add a function <code>merge_tags(old_tag, new_tag)</code></strong> that replaces all occurrences of old_tag with new_tag across all contacts</li><li><strong>Add a function <code>get_contacts_without_tags()</code></strong> that returns contacts with no tags</li></ol>",
      "starter_code": "# Use the contacts database from the main example\n\ndef export_by_tag(tag):\n    \"\"\"Return sorted list of names with the given tag.\"\"\"\n    # Your code here\n    pass\n\ndef find_duplicates():\n    \"\"\"Find contacts with duplicate phone or email.\"\"\"\n    # Your code here\n    pass\n\ndef merge_tags(old_tag, new_tag):\n    \"\"\"Replace old_tag with new_tag in all contacts.\"\"\"\n    # Your code here\n    pass\n\ndef get_contacts_without_tags():\n    \"\"\"Return contacts that have no tags.\"\"\"\n    # Your code here\n    pass\n\n# Test your functions\nprint(\"Testing new features...\")\n"
    },
    {
      "role": "The Coder",
      "title": "Solution",
      "content": "<h3>✅ Solution: Extended Contact Manager Features</h3>",
      "code": "def export_by_tag(tag):\n    \"\"\"Return sorted list of names with the given tag.\"\"\"\n    names = [contact['name'] for contact in contacts.values() \n             if tag in contact.get('tags', set())]\n    return sorted(names)\n\ndef find_duplicates():\n    \"\"\"Find contacts with duplicate phone or email.\"\"\"\n    duplicates = {'phone': {}, 'email': {}}\n    \n    for cid, contact in contacts.items():\n        phone = contact['phone']\n        email = contact['email']\n        \n        # Track phone duplicates\n        if phone not in duplicates['phone']:\n            duplicates['phone'][phone] = []\n        duplicates['phone'][phone].append((cid, contact['name']))\n        \n        # Track email duplicates\n        if email not in duplicates['email']:\n            duplicates['email'][email] = []\n        duplicates['email'][email].append((cid, contact['name']))\n    \n    # Filter to only actual duplicates (more than 1)\n    phone_dupes = {k: v for k, v in duplicates['phone'].items() if len(v) > 1}\n    email_dupes = {k: v for k, v in duplicates['email'].items() if len(v) > 1}\n    \n    return {'phone': phone_dupes, 'email': email_dupes}\n\ndef merge_tags(old_tag, new_tag):\n    \"\"\"Replace old_tag with new_tag in all contacts.\"\"\"\n    count = 0\n    for contact in contacts.values():\n        if old_tag in contact.get('tags', set()):\n            contact['tags'].discard(old_tag)\n            contact['tags'].add(new_tag)\n            count += 1\n    return count\n\ndef get_contacts_without_tags():\n    \"\"\"Return contacts that have no tags.\"\"\"\n    return {cid: contact for cid, contact in contacts.items() \n            if not contact.get('tags', set())}\n\n# Testing\nprint(\"🧪 TESTING NEW FEATURES\")\nprint(\"═\" * 60)\n\nprint(\"\\n1. Export by tag:\")\nwork_names = export_by_tag('work')\nprint(f\"Work contacts: {work_names}\")\n\nprint(\"\\n2. Find duplicates:\")\ndupes = find_duplicates()\nif dupes['phone']:\n    print(\"Phone duplicates found:\")\n    for phone, contacts_list in dupes['phone'].items():\n        print(f\"  {phone}: {[name for _, name in contacts_list]}\")\nelse:\n    print(\"No phone duplicates\")\n\nprint(\"\\n3. Merge tags:\")\ncount = merge_tags('developer', 'engineer')\nprint(f\"Merged 'developer' → 'engineer' in {count} contacts\")\n\nprint(\"\\n4. Contacts without tags:\")\nno_tags = get_contacts_without_tags()\nprint(f\"Contacts with no tags: {len(no_tags)}\")",
      "output": "🧪 TESTING NEW FEATURES\n════════════════════════════════════════════════════════════\n\n1. Export by tag:\nWork contacts: ['Alice Johnson', 'Carol Davis']\n\n2. Find duplicates:\nNo phone duplicates\n\n3. Merge tags:\nMerged 'developer' → 'engineer' in 1 contacts\n\n4. Contacts without tags:\nContacts with no tags: 0",
      "explanation": "<h3>📚 How the Solutions Work:</h3><ol><li><strong>export_by_tag() - List Comprehension + Sorted:</strong><pre><code>names = [contact['name'] for contact in contacts.values() \n         if tag in contact.get('tags', set())]\nreturn sorted(names)</code></pre><ul><li>Filter contacts by tag membership</li><li>Extract just names</li><li>Sort alphabetically</li></ul></li><li><strong>find_duplicates() - Grouping Pattern:</strong><ul><li>Build dict of phone → list of contacts</li><li>Build dict of email → list of contacts</li><li>Filter to only entries with 2+ contacts</li></ul></li><li><strong>merge_tags() - Set Operations:</strong><pre><code>contact['tags'].discard(old_tag)  # Remove old\ncontact['tags'].add(new_tag)      # Add new</code></pre></li></ol>"
    },
    {
      "role": "Summary",
      "title": "Key Takeaways",
      "content": "<h3>🎯 What You've Learned:</h3><ol><li><strong>Real-world data modeling:</strong><ul><li>Nested dictionaries for complex records</li><li>Sets for tags/categories</li><li>IDs as dictionary keys</li></ul></li><li><strong>CRUD operations:</strong><ul><li>Create: add_contact()</li><li>Read: get_contact(), search functions</li><li>Update: update_contact()</li><li>Delete: delete_contact()</li></ul></li><li><strong>Search and filter patterns:</strong><ul><li>Dictionary comprehensions for filtering</li><li>Set operations for tag logic</li><li>String matching for search</li></ul></li><li><strong>Data analysis:</strong><ul><li>Counting with dictionaries</li><li>Aggregating with sets</li><li>Statistics generation</li></ul></li></ol><p><strong>This project demonstrates:</strong></p><ul><li>✅ How dictionaries model real-world entities</li><li>✅ How sets handle unique collections (tags, domains)</li><li>✅ How comprehensions simplify data processing</li><li>✅ How to build a complete, functional application</li></ul><h3>🚀 Module 7 Complete!</h3><p>You've mastered Python's most powerful data structures! Ready for Module 8: Error Handling!</p>"
    }
  ],
  "checkpoint_quiz": [
    {
      "question": "What data structure is best for storing unique tags for a contact?",
      "options": [
        "List",
        "Dictionary",
        "Set",
        "Tuple"
      ],
      "correct_answer": 2,
      "explanation": "A SET is best for tags because: (1) tags should be unique - no duplicates, (2) order doesn't matter, (3) fast membership testing for 'has this tag?'. Lists allow duplicates, dicts need key:value pairs, and tuples are immutable."
    },
    {
      "question": "How would you find all contacts with a specific tag using dictionary comprehension?",
      "options": [
        "{c for c in contacts if tag in c}",
        "{cid: c for cid, c in contacts.items() if tag in c['tags']}",
        "[c for c in contacts if tag in c['tags']]",
        "{tag: c for c in contacts}"
      ],
      "correct_answer": 1,
      "explanation": "Option B is correct: {cid: c for cid, c in contacts.items() if tag in c['tags']}. It creates a dict with contact IDs as keys, filters by checking if tag is in the contact's tags set, and uses .items() to get key-value pairs. Option A doesn't access tags, C creates a list not dict, D doesn't filter."
    },
    {
      "question": "Why use .get() instead of direct access when retrieving contacts?",
      "options": [
        "It's faster",
        "It prevents KeyError if contact doesn't exist",
        "It's required by Python",
        "It makes the code shorter"
      ],
      "correct_answer": 1,
      "explanation": "Use .get() to prevent KeyError when the contact ID doesn't exist. contacts[id] crashes if id isn't found, but contacts.get(id) returns None (or a default). This makes code more robust and easier to handle missing data gracefully."
    }
  ]
}
